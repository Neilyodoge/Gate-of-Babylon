# 开发待办（Backlog）

> **用途**：跟踪 v0.6 之后“做了哪些 / 还差哪些”。完整设计以 [主 GDD](GDD_秘境探索.md) 为准，阶段规划见 [Demo 路线图](Demo路线图.md)。
> **v0.6 方向**：长局模块化 Build + Hades 式局外结算；技能系统为 `Trigger + Effect + Modifier`；独立职业 / 天赋树 / 宝石系统 / 秘术系统不再作为当前系统推进。
> **注**：下方「P0~P3」分类沿用 v0.6 词汇（被动/主动执行模式等），其中模块系统部分已被 V.08（核心技能 × 增强链 · Proc → Consume）取代，最新落地与缺口以本页顶部「V0.2 实现状态」为准。
> **范围**：本页只记录 `Babylon/Assets/1Game/` 内的游戏开发事项；URP 管线、工具库、共享资源和第三方样例等 `1Game` 外改动不写入本待办。

---

## 关卡 A · 区域式地牢目标（2026-08-03 · 设计已确认 / 未实现）

> 权威规则见 GDD §7.2、§7.5～§7.10；silverua/StsMap 仅保留为删除前回退资产，不再是默认 Provider。
> 2026-08-04：Edgar PRO Grid3D 已导入并完成 14 房实体地牢桥接原型。

- [x] **工程资源清理**：完成 `Assets/1Game` 反向依赖审计并删除约 10.5 MB 无引用 Demo、旧贴图及演示源资源；素材包备用动画/模型、动态加载资源和 silverua 回退资产继续保留。
- [x] **关卡配置 Asset 化**：秘境结构、房间内容、战斗遭遇、剧情事件和首领阶段已迁入 `关卡数据库.asset`；中文关卡工具成为唯一编辑入口，旧关卡 CSV/JSON 与 EnemySquad 回退已删除。
- [ ] **阶段 1 · 区域数据层**：建立区域节点图，支持 `Outer / Transition / Inner` 分区、房间角色、邻接关系、候选槽与显式 Boss ID。
- [x] **阶段 1a · 房间内容数据底座**：已建立 District/Role/ActivationMode/LockPolicy、固定内容 Seed 与统一房间状态；当前按稳定物理索引暂算分区，后续替换为区域节点图来源。
- [ ] **阶段 2 · 双 Boss 与故事模板**：外环/内环各生成一个 Boss；拆分 Layout Template 与 Story Template，并按兼容标签组合。
- [ ] **阶段 3 · 房间常驻与探索**：已完成一次生成、步行进房触发、锁门/清场解锁、已清房回访、双 Boss 条件出口；继续补区域标签、捷径和双 Boss 候选位/任意顺序的正式数据层。
- [x] **阶段 4a · 实体模块拼接原型**：导入 Edgar PRO Grid3D；游戏侧 14 房/3 房间模板/2 走廊模板生成、碰撞、门匹配与运行桥接验证通过。
- [x] **阶段 4b · 首批游戏美术房间模板**：使用 Fantastic Dungeon Pack URP 模块拼装 6 个房间、2 个走廊和门连接/封堵件；8 个模板诊断通过，固定 Seed 可生成 14 房。
- [ ] **阶段 4c · 美术模板扩充**：按 GDD §7.2.2 将当前 6 主体房 + 2 走廊补齐至 **12 主体房 + 4 连接模板**。现有 6 主体房重装为牢房 A/B、仓库刑具、外环 Boss、祭坛大厅、墓室；新增铁闸门厅、地下岔路厅、行商前哨、图书静修室、军械库、内环 Boss 共 6 房；现有走廊定为直线/直角，新增 T 形岔路与楼梯高差。
- [ ] **阶段 5 · 内容注入**：普通小怪已改为三分区敌池 + 威胁预算 + 近战/远程/法术比例自动组队，精英怪暂不进入自动池；Boss 按分区/房间标签/事件 Flag 独立抽取；接入五种 SpawnMode、范围/离散点刷新、独立计数与最多一批增援。继续补材料槽、内环军械库装备 A、事件槽与装饰注入。
- [x] **阶段 5a · 中文制作工具**：完成普通小怪、区域配比、首领、秘境结构、房间内容、遭遇、剧情事件和首领阶段配置，以及房间有效范围、方盒/网格怪物刷新范围、内容标记和 Edgar 基础诊断工具。
- [x] **阶段 5b · 日常模式收口**：默认只暴露怪物生成、首领随机和房间制作；小怪/首领列表及三区数量配比改为精简编辑卡片，其余底层规则统一放入可选高级模式。
- [ ] **阶段 6 · 地图 UI**：由 STS 路线选择图改为探索地图，显示已发现房间、门、捷径和两个 Boss 进度。

### 待确认 / Playtest

- [ ] 双 Boss 是否必须全部击败，还是允许击败一个 Boss 后提前结算；首版按“全部击败开启出口”实现。
- [ ] 军械库距离主路 1～2 房是否既能形成探索感，又不会导致装备 A 过度漏拿。
- [ ] 材料的局外用途、死亡损失比例、稀有材料保护与基地建设范围。
- [x] 关卡 A 首版固定 14 个主体房：普通 5 / 精英 2 / 事件 2 / 商店 1 / 休息 1 / Boss 2 / 军械库 1。
- [ ] Playtest 14 房目标下的单局时长与普通/精英比例；允许调比例，不改变双 Boss、双事件、单军械库和三分区结构。
- [ ] 首批故事模板数量，以及每组故事模板的外环 Boss、内环 Boss 和必选事件。
- [ ] Playtest Fantastic Dungeon 模板的玩家出生高度、敌人落点、门锁碰撞、清场解锁及 4m 美术网格与当前整体缩放的手感。
- [ ] Playtest 敌人插槽密度、入口 5m 安全距离、0.75s 波次间隔、Boss 房随从数量及跨房间清场隔离。
- [x] 固定 Seed `20260805` 自动烟测：14 房生成、枚举/引用解析、五种 SpawnMode 预备、CombatLock、Dormant→Active→Completed 与回访不重刷通过；仍需真人走房验证伏击提示、巡逻手感和增援节奏。
- [ ] 使用中文关卡工具将现有 6 个正式房间逐步补上有效范围和怪物刷新范围，Playtest 自动配比、Mesh表面落点、门口安全距离及远程/法术站位。
- [ ] Playtest 世界掉落密度：敌人即时掉落 + 清场技能/模块掉落叠加后，是否过量以及是否影响地面可读性。

---

## V0.4.1 §11.4 方案修订（2026-08-03 · 设计已确认）

- [x] GDD 改为“局内 Build 不带出，首次获取技能/模块即永久解锁”。
- [x] 取消当前规划中的大秘境、Build 快照、Build 背包和局外 Build 装备。
- [x] 明确玩家进度使用 3 个独立存档槽，选择界面纵向排列。
- [x] 按 GDD §11.4.8 清理旧 Build/大秘境代码，并落地永久解锁与纵向存档 UI。

---

## V0.4.1 §11.4.7 补充反馈（2026-07-30 · 4 项已处理）

- [x] 初始技能选择后移：准备区不自动弹 UI，触发正式入口后选择，选完才进入局内。
- [x] 普通秘境准备区保留返回基地出口；旧大秘境缓冲区及其返回出口已随 Rift 流程删除。
- [x] 每境总节点 24~26（含共享 Boss）拆为 3 条长短不同路线；地图 outgoing 成为路线结束真源。
- [x] 模组获取修复：`ModuleCatalog` 收录 59 个模块；三选一首战给模块，之后技能/模块交替；自动装配失败时进入模块背包（奖励/商店均不丢失）。

### 待 playtest

- [ ] 确认单条路线约 8~10 间（含共享 Boss）的实际局长是否符合节奏；总图节点仍保持 24~26。
- [ ] 确认准备区返回基地后再次入场，技能栏保持挂空且地图重新生成。
- [ ] 确认模块三选一进入背包后可在模块装配 UI 正常安装。

---

## V0.4.2 三向 UE 风格 PBR Shader（2026-07-30 · 已处理）

- [x] 三向采样 Shader 重命名为 `Game_TripPBR.shader`，删除旧 Half-Lambert 版本。
- [x] UE 风格 BRDF 独立到 `Game_Lighitng.hlsl`，供后续游戏 Shader 复用。
- [x] Mask 贴图通道统一为 SMAE：R 光滑度 / G 金属度 / B AO / A 自发光 Mask。
- [x] 适配 Unity/URP 光强单位，移除重复的漫反射 `1/π` 衰减，使亮度基准接近默认 Lit。
- [x] 原测试材质迁移为 `Game_TripPBR.mat`，Unity 编译验证 0 消息。

---

## V0.4.1（工程规范）场景搭建合规化（2026-07-30 · 已处理）

> 细节见 [CHANGELOG · 场景搭建合规化](../CHANGELOG.md)。

- [x] 后处理改走纯 Unity 默认：删 `PostProcessSetup`（及 `GameManager`/`PlayerController` 调用），新增 `Demo1PostProcess.asset`（VolumeProfile），场景「Art/Global Volume」标准 Volume 引用之。
- [x] 接入 URPFog `urp14` 高度雾：嵌入式 UPM 包 + `N_RenderData` Renderer Feature + `N_VolumeProfile` Height Override；初始强度为 0，待按地牢深渊场景调色与性能验证。
- [x] 美术对象场景预置：`Main Camera`+`TopDownCamera` / `Directional Light` / `Global Volume` 放进 Demo1 场景「Art」节点，Inspector 直接调参，不再运行时实例化（`Demo1Setup` 仅保留相机/光兜底）。
- [x] 运行时实例化按类别拆分：新增 `SystemsBuilder`/`GameplayBuilder`/`HudBuilder`，场景 `Systems`/`Gameplay`/`UI` 节点各挂其一；`Demo1Setup` 瘦身为 Bootstrap 按序调度。

### 待观察 / 后续

- [ ] 若后续要做「随层数变氛围 / 受击暗角脉冲」，改为标准做法：新增独立控制器读取场景 Volume 的 Profile override（不再代码拼 Profile）。
- [ ] HUD 目前仍由 `HudBuilder` 运行时代码搭建；若要更彻底合规，可后续改为「预制体化 + 引用装配」。

---

## V0.4.1 §11.4.6 BUG 及调整整理（2026-07-30 · 9 项已全部处理）

> 逐项修复 GDD §11.4.6 表。以下是历史落地记录；#5～#7 及 #8 的大秘境分支已被 2026-08-03 新方案废弃，清理任务见本页“旧 Build 存档与大秘境实现”。

- [x] #1 局外去技能：`EnterVillageHub.ClearPlayerLoadout()` 清 Q/E/R + 增强链（保留普攻）。
- [x] #2 完全无掉落：总开关 `GameManager.EnableWorldDrops=false`，所有 `*.Spawn` early-return；碎片货币保留。
- [x] #3 每关房间数 20~26：旧方案曾将 `layers` 12→24；已被 §11.4.7 的“每境总节点 24~26、拆为三路线”方案取代。
- [x] #4 统一 3 关：换境首间也走 STS 全图（`EnterNextRoomWithChoice`）；HUD 改「第 X/N 关」。
- [x] #5 装备台刷新技能栏：`BuildSnapshot.ApplyToPlayer` 广播 `SkillEquipped` + 刷新 `SkillBarUI`。
- [x] #6 大秘境选层：新增 `RiftTierSelectUI`（1~100 层），装备后进挑战前弹出。
- [x] #7 局外剔除 BD：`VillageHub` 去掉配置使/Build 管理使，只留大秘境入口 + 山门。
- [x] #8 局外不显示地图：`Minimap.SetVisible`，进村/进大秘境隐藏。
- [x] #9 暂停菜单存档：`PauseMenu`「存档」按钮 → `SaveSystem.Save()`。

### 待观察 / 后续

- [x] 原“每条路线 24 房导致单局过长”已由 §11.4.7 路线预算修复；当前单路线约 8~10 间。
- [ ] 宝箱房（Treasure）在「无掉落」下开箱不再产出，暂显空；后续若保留该房型可改为直接给碎片或并入三选一。
- [ ] 遗产模块（`lastRunLegacyModuleId`）走世界掉落，已被总开关屏蔽；后续如需保留可改由三选一奖励池发放。

---

## V0.4.1（进度重构线）接入 silverua 杀戮尖塔原生全图（2026-07-30）

> 地图彻底换成 silverua 原生生成+渲染，地图=进度真源、每局重生成、DOTween 动效。细节见 [CHANGELOG · V0.4.1](../CHANGELOG.md)。

### 已做

- [x] **落地资产**：silverua Scripts/Prefabs/Sprites/Materials/Resources/SO + DOTween → `1Game/StsMap/`；剔 Newtonsoft（不持久化）+ OneLine。
- [x] **表现层** `StsMapScreen`：运行时搭 Overlay Canvas + 双 ScrollRect + 实例化 `Resources/StsMapObjectsUI`，回填 `MapViewUI` ScrollRect。
- [x] **桥接** `SilveruaMapProvider : IMapProvider` + `MapPlayerTracker.NodeEntered` 事件（进入延时改非缩放时间）；`NodeType→RoomType` 映射；数值走 `ConfigDatabase.MapStructures`。
- [x] **GameManager**：`Awake` 换 provider；首房经地图点选；`RealmCount`；遗产模块改首个 `SpawnCurrentRoom` 注入。
- [x] Unity 运行验证：生成/渲染/选点→回调→隐藏链路均通（`timeScale=0` 亦生效）；编译 0 错。

### 遗留清理（2026-07-30 已处理）

- [x] **物理删除**旧地图代码：`TreeMapGenerator`+`TreeMap`/`TreeNode`（`LevelDesign/Map/`）、`TreeMapUI`、`LevelDesignMapProvider`（含 .meta；空 `Map/` 目录一并删）。
- [x] **解耦 `LevelDesignDirector`**：去掉 `CurrentMap`/`ShowMap`/`CurrentMapNode`/`MarkCurrentNodeCleared`/`IsCurrentNodeBoss`/`TryTriggerRoomEvent`；只保留「整局/区域 Flag & 玩家状态重置」+「Boss 形态解析」。
- [x] **迁移重置职责**：整局/换境的 Flag/状态重置从 `LevelDesignBootstrap.RealmBreakthrough` 移到 `SilveruaMapProvider.StartRun/OnEnterRealm`（调 `Director.StartNewRun/BeginAct`）；`Bootstrap` 只保留线性事件调度（mode B）+ 区域通关 meta 标记。
- [x] `MapProviders.Current` 默认值改 `SilveruaMapProvider`。
- [x] **`MapConfig` 按 Act 分化（预留）**：`StsMapScreen.SetActConfig(act)` 从 `MapViewUI.allMapConfigs` 按 (act-1) 选配置；provider 在 StartRun/OnEnterRealm 调用。目前列表仅 1 个 `DefaultMapConfig`（各境共用），要分化只需把对应 `MapConfig` 追加进列表，无需改代码。
- [x] Unity 运行验证：provider 重置/事件/Floors 链路 smoke 全绿，编译 0 错。

### 保留（有意为之）

- `LevelRoomType` 与 `RoomType` **不合并**：`LevelRoomType` 是配表 schema 枚举（`ConfigTables.RoomSocketRow.TypeEnum`，绑定 `Room_Socket_Group_Config.csv` 的 RoomType 整数列），非死代码；合并等于改配表数据/序数，超出清理范畴。silverua 侧只用 `NodeType→RoomType`，已不再触碰 `LevelRoomType`。

---

## V0.4.6 UI 方案统一为 uGUI + TMP（2026-07-29）

> 全项目 UI 从 UITK/uGUI/IMGUI 三套混用收敛到单一 **uGUI + TextMeshPro**。细节见 [CHANGELOG · V0.4.6](../CHANGELOG.md)。

### 已做

- [x] **基建**：`UGuiKit` 统一构建库 + 中文动态 TMP 字体（`NotoSansSC SDF`）+ TMP Essential Resources 修复。
- [x] **UITK→uGUI**：MainMenu(试点)/PauseMenu/SettingsUI/RewardPickUI/SkillSelectUI/SaveSlotSelectUI/BuildBackpackUI/PlayerInfoPanel/CodexUITK/ShopRoom/UpgradeRoom/StoryEventUI/BossDialogueUI/ExtractResultPanel/RiftEquipUI/RiftRewardUI/BuffBarUITK/TreeMapUI。
- [x] **IMGUI→uGUI**：RunHUD/RiftChamber/FormationPlatform/ScripturePavilion（运行期已无 OnGUI）。
- [x] **旧 Text→TMP（约 20 文件）**：。
- [x] Unity 编译 0 错误。

### 待做（清理 · 非承重）

- [ ] 删除已无消费者的旧 UITK 资产：`Resources/UI/*.uxml` + 同名 `.uss`（ShopRoom/BuffBarUITK/SettingsUI/PauseMenu/BossDialogueUI/TreeMapUI/ExtractResultPanel/MainMenu/UpgradeRoom/StoryEventUI/CodexUI）、`AvatarSelectPanelSettings.asset`、`UIBuiltins`（LegacyFont 已无引用）。建议用 `AssetDatabase.DeleteAsset` 连带 `.meta` 一起删。
- [ ] Demo1Setup/装配台等文本 GameObject 上遗留的 `UnityEngine.UI.Outline` 组件（对 TMP 无效但无害）可择机移除。

---

## V0.4.4 去修仙化清理 · R4（2026-07-29）

> 物理删除过期的「职业 / 化身 / 局内灵物」逻辑与资产。细节见 [CHANGELOG · V0.4.4](../CHANGELOG.md) 与 GDD §10.3 归档表 R4。

### 已做

- [x] **职业**：删多职业/起始模板选择（`StartTemplate*`/`CharacterSelectUITK` + `StartTemplates/` 8 资产 + `法修` 档案），保留单一主角档案（剑修）。
- [x] **化身**：删孤儿 UI（`AvatarSelect`/`GrowthUITK`）+ `avatarCoefficient` 战斗乘区（`StatType.LegacyCoeff` 墓碑保序）。
- [x] **灵物**：删局内灵物 SO（`Data/Items`+`Resources/Items` 各 20）+ `Item_InRun_Config` 表 + `BattleRewardUI` UI + 配表/导表/事件占位逻辑；保留 `ItemRarity` 共享枚举与洞府素材管线。
- [x] Unity 编译 0 错误；文档同步（GDD/CHANGELOG/架构说明）。

### 待做（非承重 · 可选）

- [ ] `Demo1DataCreator` 内孤儿的 `Create*Orb/Bead/...` 灵物 SO 生成方法体清理（当前无调用者，编译无害）。
- [ ] 各处 `灵物`/`化身` 历史注释与编辑器菜单文案（`ToolSearchWindow`/`ConfigDashboard`）中性化。
- [ ] 历史文档 `1Game/Docs/资源_灵物配置指南.md`、`策划_灵物与功法设计框架.md`、`docs/game/tech/灵物系统.md` 评估删除或标注归档。

---

## V0.4.1 Phase1 实现状态（2026-07-28）· 局内调整

> V0.4.1 Phase1 落地：开局无技能 / 三选一奖励 / 移除背包 / 商店扩展 / 关卡结构 3×12。细节见 [CHANGELOG · V0.4.1 Phase1](../CHANGELOG.md)。

### 程序已做

- [x] **Phase1b：开局无技能**：移除 `GrantSeedLoadout`，Q/E/R 全空（保留普攻）。
- [x] **Phase1c：三选一奖励系统**：新建 `RewardPickUI`（UITK 程序化），替代 `BattleRoom` 地面掉落。
  - 战斗房 70% / 精英房 90% / 事件房 100% 概率触发
  - 同类 3 选 1（全技能 或 全模块）
  - 技能栏满时自动弹替换确认，按稀有度折算货币
  - 模块自动装备到第一个有空位的增强链
- [x] **Phase1d：移除局内背包**：`ModuleInventory` 运行时不填充，模块直接装备到链。
- [x] **Phase1e：商店扩展**：7 件商品（2 技能 + 5 模块）+ 刷新按钮（费用递增）。
- [x] **Phase1a：关卡结构**：3 层 × 12 关，固定布局回退（精英≤2，商店≤2）。

### Phase2 存档与永久解锁（2026-08-03 方案修订）

- [x] **进度存档底层 3 槽**：`SaveSystem` 已使用 `save_slot_{0-2}.json`，并兼容旧 `save_v1.json` 迁移。
- [x] **继续游戏**：已支持加载最近使用槽位。
- [x] **存档选择 UI 改版**：`SaveSlotSelectUI` 已改为从上到下纵向展示 3 个存档卡；已有槽显示主要进度、游玩时间和最后保存时间，读取/删除/覆盖分离且破坏性操作二次确认。
- [x] **存档入口修复**：槽位按钮设置尺寸前补齐 `LayoutElement`，避免空引用并保留横向布局的可点击区域；旧 schema JSON 加载时补齐全部集合字段，恢复“进入基地”与旧档读取。
- [x] **永久解锁字段**：schema v3 在每个进度存档中保存已解锁技能 ID、模块 ID；同一 ID 幂等写入。
- [x] **获取即解锁**：初始选择、战后奖励、世界拾取、商店购买和模块入包/自动装配均在首次实际获取时立即登记并保存。
- [x] **局外解锁查看**：`CodexUITK` 按当前槽展示发现状态；未发现条目隐藏名称和详情，不提供整套局内 Build 装备。
- [x] **运行态隔离**：回基地与新开局清空技能、模块背包和增强链；新一局仍挂空开始。
- [x] **装备反馈保留**：技能装备继续广播 `SkillEquipped`；模块链变更统一刷新 `SkillBarUI`。

### 旧 Build 存档与大秘境实现（已废弃，清理完成）

- [x] 删除 `BuildSnapshot` / `ChainSnapshot` 与 `SaveBuildFromCurrentRun()`；`buildBackpack` 仅保留为旧 JSON 迁移墓碑，加载后清空并写回 schema v3。
- [x] 删除 `BuildBackpackUI`、`RiftEquipUI` 及 Build 管理/装备交互入口。
- [x] 删除 `RiftEntrance`、`RiftManager`、`RiftBufferRoom`、`RiftChamber`、`RiftRewardUI`、`RiftTierSelectUI` 与 `GameManager.InRift` 分支。
- [x] 确认场景/预制体无硬引用；清理运行时入口和存档业务引用，保留普通秘境准备区返回基地功能。
- [x] 当前规划不再建设大秘境奖励、配表、排行和词缀。

---

## V0.4.0 实现状态（2026-07-15）· 战斗/关卡/系统全面重构

> V0.4 全量落地：删除职业选择 / 技能三选一 / 准备房间 / 删除撤退 / 模块掉落+商店 / 关卡扩充 / C 键信息面板 / 主题文案替换。细节见 [CHANGELOG · V0.4.0](../CHANGELOG.md)。

### 程序已做

- [x] **前置：主题替换**：全部修仙文案→通用冒险/奇幻词汇（UI / HUD / 结算 / 商店 / 调试日志）。
- [x] **前置：V0.3 清理**：GDD V0.3 版本计划 + 版本表记录行删除。
- [x] **战斗1：删除职业选择**：移除 StartTemplate 流程，VillagePortal 直接入秘境，初始无技能。
- [x] **战斗2：技能三选一**：新增 `SkillSelectUI`（UITK），最低品质技能 3 选 1 装备到 Q 槽。
- [x] **关卡1：准备房间**：新增 `PrepRoom` + `PrepRoomExit`，局外→准备→选技能→局内。
- [x] **关卡2：删除撤退**：移除 ExtractPoint + 搜打撤逻辑，标准 roguelike 层间过渡。
- [x] **关卡3：模块掉落+商店**：`GameConfig` 配置驱动掉落概率/数量；ShopRoom 新增模块商品。
- [x] **关卡4：关卡扩充**：每层统一 11 房间 + Map_Structure_Config 三 Act 统一结构。
- [x] **系统1：C 键信息面板**：PlayerInfoPanel Tab→C，解决 DebugConsole 键位冲突。

### 待做（V0.5+）

- [ ] Playtest 验证：完整局体验流程（准备房间→10+房间/层→通关），检查节奏和难度曲线
- [ ] 技能三选一：扩充最低品质技能池（当前仅 3 个 Fan 品质技能，恰好够 3 选 1 但无随机性）
- [ ] PrepRoom 功能扩展：后续添加模块配置、信息查看等准备阶段功能
- [ ] ShopRoom 模块购买后的 UI 反馈（购买动画、模块装备提示等）
- [ ] 模块掉落概率的 CSV 配表化（当前在 GameConfig SO 中，未来迁至 CSV）
- [ ] StartTemplate / StartTemplateSelectUI 文件清理（已为死代码，可安全移除但需处理 .meta）
- [ ] ExtractPoint / CaveInventory 相关代码的深度清理（当前保留文件但核心引用已切断）

---

## V0.3.0–V0.3.4 实现状态（2026-07-14）· 系统交互重构 + UI 优化

> V0.3 全量落地：信息面板 / 局内装配提示 / 职业选择移至山门 / 图鉴 / HUD 优化。细节见 [CHANGELOG · V0.3.0–V0.3.4](../CHANGELOG.md)。

### 程序已做

- [x] **V0.3.0 信息面板**：`PlayerInfoPanel`（UITK，Tab 键切换，展示属性/成长/增强链）。
- [x] **V0.3.1 局内装配提示**：底部操作提示更新（M 装配 / Tab 信息 / ESC 暂停）。
- [x] **V0.3.2 职业选择移至山门**：主菜单→直接进基地，山门按 F→弹模板选择→入秘境。
- [x] **V0.3.3 图鉴功能**：`CodexUITK` 重写（模块/技能双 Tab + 分类筛选 + 稀有度展示）。
- [x] **V0.3.4 HUD 优化**：Buff 栏下移 / 道心面板避让小地图 / 敌人计数下移 / 小地图图例。

---

## V0.2.5 实现状态（2026-07-10）· 单局节奏压缩 + Event 叙事全覆盖

> 三 Act 总楼层 24→12，单局 25-40min。Act2/Act3 事件房有完整叙事内容。细节见 [CHANGELOG · V0.2.5](../CHANGELOG.md)。

### 程序已做

- [x] **Map_Structure_Config 压缩**：Act1=3层 / Act2=4层 / Act3=5层（总 12 层）。
- [x] **运行时计时器**：`GameManager.RunElapsedSeconds` + 结算面板显示"探索时长"。
- [x] **Event_Story_Config 填充**：新增 8 个事件（Act2: 2001-2004 / Act3: 3001-3004），含条件分支链。
- [x] **Room_Socket_Group_Config 扩展**：Act2(ID 7-13) / Act3(ID 14-20)，每 Act 独立 EventID 池。
- [x] **Map RoomPoolID 更新**：Act2/Act3 指向各自的 Socket 组。
- [x] **线性事件调度表更新**：`LevelDesignBootstrap._linearEventSchedule` 覆盖 6 个 realm。

### 待做（V0.3+）

- [ ] 实际 Playtest 验证（手动跑完整局测时长）
- [ ] Event RewardID（4001/5001/6001 等）对应的实际奖励逻辑
- [ ] 条件事件的后续链（如 2003 依赖 read_ghost_tablet → 需验证事件链闭合）
- [ ] 天火试炼的概率结果分支（60%成功/40%失败）实际逻辑

---

## V0.2.4 实现状态（2026-07-10）· Boss 动态化 + 通关结算闭环

> BossID 按 Act 动态传入，P2 形态配表驱动，通关走统一结算面板，三 Act Boss 美术已接入。细节见 [CHANGELOG · V0.2.4](../CHANGELOG.md)。

### 程序已做

- [x] **`EnemyBoss.Spawn` 新增 `bossID` 参数**：不再硬编码 1，由 `GameManager.SpawnBossRoom` 传入 `ActID`。
- [x] **P2 配表驱动**：Spawn 时缓存 `BossPhaseSelector.Select(bossID).Phase2`；50% HP 切换时应用 `StatModifier` 而非硬编码 ×1.3。
- [x] **通关结算闭环**：`isLastRealm` 分支改为调用 `ExtractResultPanel.Show(EndType.Victory, ...)`，×2.0 倍率 + 遗产选择 + 返回洞府。
- [x] **Boss_Phase_Config 填充 Act2/Act3**：幽冥谷守灵(5 形态) + 炼狱峰龙魂(5 形态)，含独立对白/数值修正/召唤。
- [x] **Boss 多形态美术接入**：`MonsterPrefabs` 新增 `Boss_Act2_Prefab`(Dragon Nightfall) / `Boss_Act3_Prefab`(Dragon Dusk)，`EnemyBoss.Spawn` 按 bossID 选取。
- [x] **Flag 系统扩展**：`kill_count` 累计击杀、`cleared_act{N}_no_death` 无死通关标记、`MarkDeath()` 死亡标记。

### 待做（V0.2.5+）

- [ ] 单局时长 Playtest 验证（25-40min 目标）
- [ ] Event 房叙事内容填充
- [ ] 召唤 SquadID 实际怪物配置（SummonSquadID 2/3 的具体怪物表）
- [ ] Boss 动画 AnimatorController 统一配置（Attack/Idle/Die 状态机）

---

## V0.2.2 实现状态（2026-07-09）· 统一结算 + 遗产系统

> 统一所有局终出口的经验结算 + 遗产模块跨局传承。细节见 [CHANGELOG · V0.2.2](../CHANGELOG.md)。

### 程序已做

- [x] **`InsightSystem.CommitOnDeath(0.5f)`**：死亡保留 50% 经验（不再全丢）。
- [x] **`ExtractResultPanel` 重构**：新增 `EndType` 枚举 + 遗产模块选择 UI（卡片网格，选 1 件金色高亮）。
- [x] **`SaveDataV1.lastRunLegacyModuleId`**：跨局遗产模块存档字段。
- [x] **`GameManager.OnPlayerDied` 重做**：弹结算面板（0.5x + 遗产选择），不再静默处理。
- [x] **`GameManager.TryInjectLegacyModule()`**：下局首战自动生成遗产 `ModulePickup`，一次性使用。
- [x] **撤离结算同步**：`SpawnExtractPointAndPortal` 回调传入 `legacyModules` + `EndType.Extract`。

### 待做（V0.2.3+）

- [ ] 通关结算面板（`EndType.Victory`，经验 ×2.0）
- [ ] 单局时长 Playtest 验证（25-40min 目标）
- [ ] Boss 多形态接入
- [ ] Event 房叙事内容填充

---

## V0.2.1 实现状态（2026-07-09）· 房间类型全闭环 + 稀有度联动 + 阶段返回

> V0.2.0 的 TreeMap 骨架已接入主循环。V0.2.1 让所有房间类型可运行，不再退化映射。细节见 [CHANGELOG · V0.2.1](../CHANGELOG.md)。

### 程序已做

- [x] **`Minimap.RoomType` 扩展**：新增 `Elite`、`Event` 枚举值 + 小地图颜色/图标。
- [x] **`MapLevelRoomToMinimap` 更新**：Elite→Elite（不再退化 Battle），Event→Event（不再退化 Treasure）。
- [x] **`SpawnEliteRoom`**：强化敌人（精英倍率×层缩放）+ 3 件高稀有度模块掉落。
- [x] **`SpawnEventRoom`**：构建房间视觉 + 触发 `StoryEventService` 叙事事件 + 完成后 RoomCleared。
- [x] **`ModuleDropWeighting.PickWeighted(pool, rarityBias)`**：新增稀有度偏移参数，高品阶模块权重递增。
- [x] **`BattleRoom.SpawnModuleReward` 接入 `ModuleRarityBias`**：读配表层偏移 + 精英房额外 +20。
- [x] **`ShouldShowStageReturn()`**：读配表条件化阶段返回点，无返回层直接传送下一层。

### 待做（V0.2.2+）

- [ ] 阶段返回结算面板升级（经验倍率 + 模块保留选择 UI）
- [ ] 遗产系统（`lastRunLegacyModuleId` 注入首战斗房）
- [ ] 单局时长 Playtest 验证（25-40min 目标）
- [ ] Boss 多形态接入
- [ ] Event 房叙事内容填充（目前依赖 StoryEventService 已配置的事件）

---

## V0.2.0 实现状态（2026-07-09）· 关卡生成配表 + 程序化地图接入主循环

> V0.2 正式启动。把已有 `LevelDesignDirector`/`TreeMapGenerator` 从旁路接入 GameManager 主循环。细节见 [CHANGELOG · V0.2.0](../CHANGELOG.md)。

### 程序已做

- [x] **`Map_Structure_Config.csv` 扩展**：新增 `EnemyScaleMul`/`ModuleRarityBias`/`HasStageReturn` 3 列。
- [x] **`MapStructureRow` 扩展**：新增 3 字段 + 便捷访问器。
- [x] **`CsvToJsonImporter` 扩展**：新增 `ParseFloatArray` + 解析新字段。
- [x] **`GameManager.StartNewRun()` 接入**：TreeMap 驱动房间序列（兜底旧固定布局）。
- [x] **`GameManager.SpawnBattleRoom()` 读表**：敌人倍率乘配表每层缩放。
- [x] **`GameManager.OnRoomCleared()` 同步**：标记 TreeMap 节点完成。
- [x] **`LevelDesignBootstrap` 防重复**：GameManager 已生成地图时跳过。

### 待做（V0.2.1+）

- [x] 精英房/事件房内容完善 ——已于 V0.2.1 完成
- [x] `ModuleRarityBias` 接入掉落权重 ——已于 V0.2.1 完成
- [x] 阶段返回点 UI（`HasStageReturn[floor]=1` 时弹出面板）——已于 V0.2.1 完成（条件化）
- [ ] 遗产系统（`lastRunLegacyModuleId`）
- [ ] 单局时长 Playtest 验证（25-40min 目标）
- [ ] Boss 多形态接入

---

## V0.1.18d 实现状态（2026-07-08）· 核心技能表补全

> 战斗配表体系收官：技能主表 + 参数仓库表。细节见 [CHANGELOG · V0.1.18d](../CHANGELOG.md)。

- [x] **`Skill_Param_Config.csv`（24 行 54 列）**：从 `SkillData` 资产真实导出全字段（充能/蓄力/投射/位移/治疗/召唤/Buff/Zone/表现），主键 `ConfigId`=`Skill_Base_Config.ID`。
- [x] 接入 `ConfigTables`/`Importer`/`ConfigDatabase.GetSkillParam`；索引表登记；验证 `SkillParams=24`。
- [x] 深度=作表 + 可导 JSON + 可加载；运行时仍以 SO 为准（零回归）。
- 备注：`金钟罩`(configId=0) 与特殊型 IDs 16-20（无对应 SO）不在本表；如需再补。

---

## V0.1.18c 实现状态（2026-07-08）· 运行时改为读表

> 把配表从「可加载」升级为「运行时生效」。三处均带安全回退，1:1 零回归。细节见 [CHANGELOG · V0.1.18c](../CHANGELOG.md)。

### 程序已做

- [x] **ConsumeKind 系数读表**：`ModuleChain` 读 `ConsumeKind_Bonus_Config`，回退常量。
- [x] **敌人倍率读表**：`Enemy{Base/Mage/Ranged/Charger/Boss}.Spawn` HP/伤害/防御倍率读 `Enemy_Base_Config`（回退字面量）；精英沿用 `GameConfig` 精英倍率。
- [x] **模块数值读表**：`ModuleTableApplier` 在 `GameManager.SetupModules`（真实 `modulePool` 解析后）用参数仓库表覆盖 `ModuleDef`，仅 Play 模式、缺行回退。
- [x] **验证已通过**：编译 0 error；三处读表数值正确；Play 中真实 59 池篡改后覆盖器还原为表值。

### 待做

- [x] 核心技能表补全（`SkillData` SO 全字段）——已于 V0.1.18d 完成（`Skill_Param_Config`）。

---

## V0.1.18b 实现状态（2026-07-08）· 模块参数仓库表

> 延续 V0.1.18，把模块四大类的完整数值参数各拆一张仓库表（主表只留身份/标签/关键参数）。细节见 [CHANGELOG · V0.1.18b](../CHANGELOG.md)。

### 程序已做

- [x] **`Module_Trigger_Param_Config.csv`（13 行）**：触发器全参数，自 `ModuleDef` 真实导出。
- [x] **`Module_Effect_Param_Config.csv`（20 行）**：效果器全参数（治疗/护盾/减速/眩晕/DoT/召唤/位移/易伤等）。
- [x] **`Module_Modifier_Param_Config.csv`（21 行）**：改造件全参数（burn/freeze/lightning/poison/extraCount/cost）。
- [x] **`Module_Universal_Param_Config.csv`（5 行）**：万能件双面参数 + 描述。
- [x] **导表管线接入**：`ConfigTables` 4 结构 + `Importer` 4 解析 + `ConfigDatabase` 4 字典 + `GetModule*Param`；验证 `Trig=13 / Eff=20 / Mod=21 / Uni=5`（合计 59）。
- [x] `Combat_Table_Index.csv` 4 表状态「计划」→「启用」。
- [x] 深度=作表 + 可导 JSON + 可加载；运行时仍以 SO 为准（零回归）。

---

## V0.1.18 实现状态（2026-07-07）· 战斗配表基础设施

> 落地 GDD §11.3「V0.1.14 版本计划」第一步：把模块做成策划可用 CSV 表。细节见 [CHANGELOG · V0.1.18](../CHANGELOG.md)。

### 程序已做

- [x] **模块配置主表 `Module_Base_Config.csv`**：从 59 个 `ModuleDef` 真实导出，覆盖 GDD §5.7 字段 + 新增 `DropSource`/`UnlockCond`。
- [x] **`Combat_Table_Index.csv`**：战斗全部配表索引（现有 8 张 + 模块主表 + 枚举图例 + 敌人表 + 消费系数表 + 4 张计划仓库表）。
- [x] **`Module_Enum_Legend.csv`**：模块表枚举列 int↔名称图例（自枚举生成）。
- [x] **`Enemy_Base_Config.csv`（§7.3）**：6 类敌人相对 `GameConfig` 基础值的倍率 + AI 参数，抽取自 `Enemy*` 脚本硬编码。
- [x] **`ConsumeKind_Bonus_Config.csv`（§5.6）**：身份三角（Single 1.25 / Window 1.10+范围 1.20 / Stacks 中性 / Auto 0.80），抽取自 `ModuleChain` 常量。
- [x] **导表管线接入**：3 张表 Row/Parser + `ConfigDatabase.GetModule/GetEnemy/GetConsumeKindBonus`；验证 `Modules=59 / Enemies=6 / ConsumeKindBonuses=4`。
- [x] 深度=作表 + 可导 JSON + 可加载；运行时仍以 SO/脚本/常量为准（零回归）。

### 待做

- [x] 4 张模块参数仓库表（触发/效果/改造/万能全参数）——已于 V0.1.18b 完成。
- [ ] 玩家基础数值/难度曲线：已由 `GameConfig` SO 承载（可 Inspector 编辑），如需 CSV 化再评估。
- [x] 运行时改为读表（模块参数表覆盖 SO、敌人倍率读 `Enemy_Base_Config`、系数读 `ConsumeKind_Bonus_Config`）——已于 V0.1.18c 完成（带回退，待 Play 验证）。

---

## V0.1.13 实现状态（2026-07-02）

> 补齐 V.08（V0.1.12）遗留缺口，全部经 Play 模式运行时验证。细节见 [CHANGELOG · V0.1.13](../CHANGELOG.md)。

### 程序已做

- [x] **模块占位图标**：无 `icon` 时按子类单字字形 + 元素/类别配色兜底（`SubtypeGlyph`/`SubtypeColor`），39 模块均有辨识度。
- [x] **consumeKind 联动**：身份加成三角（Single ×1.25 伤 / Window ×1.10 伤 +×1.20 范围 / Stacks 中性 / Auto ×0.80 伤），同源作用于增强+附加字段；装配 UI 预览体现（`ConsumeKindBonusText`）。
- [x] **TargetFarthest 目标改造**：新增 `M_远锁`，挂载且非环绕时核心投射技锁定 22m 内最远敌（`TryFindFarthestEnemyDir`），否则保持鼠标瞄准。
- [x] **Shape* 形态改造**：新增 `ShapeMode` 枚举 + `M_火墙`/`M_火环`/`M_火域`；火墙平行成墙、火环 360° 外散、火域（hybrid）飞弹落点生成持续区域（`Projectile.SetImpactZone`）。
- [x] **消费爆发层**：`PlayConsumeBurst` — 元素色爆闪点光 + 特效环 + 按 consumeKind 分级震屏（Enhancement/Addon 皆触发）。
- [x] **起始模板系统**：`StartTemplate` SO + `StartTemplateRegistry` + `StartTemplateSelectUI`（UITK）+ `MainMenu` 接入；3 默认模板（近战爆发/元素范围/投射连锁），选中重装 3 技能 + 应用档案 + 发起手模块。

---

## V.08 实现状态（2026-06-30）

> 配套：已落地的代码细节见 [CHANGELOG · V.08](../CHANGELOG.md)；权威设计见 [GDD §5](GDD_秘境探索.md#5-模块化技能系统核心技能--增强链--proc--consume)。

### 程序已做

- [x] `ConsumeKind`（Single / Window / Stacks / Auto）+ `EffectRole`（Enhancement / Addon）数据模型（ModuleDef / ChainConfig）。
- [x] `TriggerTracker` 四套消费状态机；`ModuleSlotManager` 的 Proc / Consume / Auto 自动释放。
- [x] 局内逐件装配 + 部分链持久化（半成品链不丢失，显示「待补」）。
- [x] 释放统一为「核心技能按键 → Proc 则注入增强 + 消费」，普通 / 蓄力 / Auto 三路径接入。
- [x] 增强型注入核心技能：伤害倍率（改造件折算）、元素覆盖、即时自益（治疗/护盾/无敌/净化）。
- [x] 增强型形态改造（部分）：`M_扩散`(RadiusScale) 放大核心范围技范围；`M_额外飞弹`(ExtraProjectile) / `M_连锁`(CountScale) 增加核心投射技发数；`M_链锁弹射`(TargetChain) 让核心投射技命中后寻敌反弹（衰减/不重复）；`M_环绕射击`(TargetSurround) 让核心投射技 360° 环绕发射；`M_余烬地带`(Sustained) 让核心范围技留下持续地带（DoT+状态）；`M_延迟轰爆`(DelayedBlast) 让核心范围技追加带预警的延迟重爆（×1.5+状态）。Debug 投射物也已携带增强 + 链锁。
- [x] 增强型控制 + 附加状态作用到核心技能**实际命中的敌人**：范围技同步捕获、投射技 payload 命中施加、未命中回退绕玩家。
- [x] 附加型 spawn 独立世界效果（控制/位移/DoT/范围/召唤等）。
- [x] 角色旁三竖条 Proc 指示器（GDD §5.13）。
- [x] 装配 UI 重做（卡片网格 + Q/E/R 竖向链 + 链效果预览 + 安装反馈）。
- [x] 开局保证 Q/E/R 三个核心技能无空窗。
- [x] 模块掉落 → 拾取 → 背包闭环（`BattleRoom.SpawnModuleReward` → `ModulePickup` → `ModuleInventory`）。
- [x] 移除「灵物 / 御灵 / 化身」旧系统出当前可玩路径。
- [x] 形态/节奏改造运行时验证（2026-07-01）：环绕 3→8 发、链锁 `_chainRemaining=2`、余烬地带生成 `ActiveSkillZone_Sustained`、延迟轰爆生成 `DelayedAreaBlast`、伤害 ×1.40 端到端全部通过。

### 程序未做 / 已知缺口

- [x] **形态改造全部落地**（TargetFarthest / Shape* 于 V0.1.13 补齐）：范围放大（RadiusScale）+ 多重投射（ExtraProjectile / CountScale）+ 链锁弹射（TargetChain）+ 环绕发射（TargetSurround）+ 持续地带（Sustained）+ 延迟爆炸（DelayedBlast）+ 最近→最远（TargetFarthest / `M_远锁`）+ 火墙/火环/火域（Shape* / `M_火墙`·`M_火环`·`M_火域`）。
- [x] **持续区域（Zone）增强**：`ActiveSkillZone.SetEnhancement` 支持元素覆盖 + 范围倍率 + 每 tick 附加状态（灼烧/冰冻/毒）；伤害倍率经 `damageMul` 生效。控制类（击退/眩晕）刻意不逐 tick 施加（避免持续弹飞），仍走绕玩家回退。
- [ ] **投射物边界**：仅带 prefab 的投射物携带增强 payload；无 prefab 的 Debug 投射物命中不施加增强（回退绕玩家）。
- [x] **consumeKind 联动**（V0.1.13）：身份加成三角接线（Single/Window/Stacks/Auto 差异化增伤/范围），装配 UI 预览体现。
- [x] **模块占位图标**（V0.1.13）：子类单字字形 + 元素/类别配色兜底（仍非真图美术，但已有辨识度）。
- [x] **消费爆发层**（V0.1.13）：爆闪点光 + 元素色特效环 + consumeKind 分级震屏。
- [x] **起始模板系统**（V0.1.13）：开局模板选择（3 技能 + 档案 + 起手模块），取代旧化身开局差异化。
- [ ] **局外结算 / 模块熟练度 / 自适应怪物生态**未实现。

---

## 当前设计基线

- **核心循环**：入关卡拼 Build → 打得更深 → 死亡 / 阶段返回 / 通关均结算经验值，只是倍率不同。
- **局外资源**：统一叫“经验值”，来源标签只影响转化方向，不再拆成多种货币。
- **角色入口**：删除独立职业；开局选择“起始模板”，后续可在局内完全转向。
- **长期成长**：删除独立天赋树；改为模块熟练度 / 解锁节点。
- **模块系统**：触发器、效果器、改造件三层组合。原宝石系统并入改造件。
- **旧实现回收**：旧化身控制器、GrowthUITK、洞府模块、技能表、灵物表可作为实现素材，但文案和系统语义需按 v0.6 重命名。

---

## P0 · 文档与配置对齐

- [x] 主 GDD 改为 v0.6：长局构筑 + 局外结算。
- [x] 简明版 GDD 同步：触发器流派、起始模板、模块熟练度。
- [x] 删除独立职业与天赋树作为现行设计。
- [x] 删除独立宝石系统，合并为改造件 / 符文。
- [x] 删除秘术系统作为当前规划。
- [~] 将配置表字段从旧语义迁移到新语义（V0.1.14 部分落地）：
  - [x] `Avatar / 化身` → 已由 `StartTemplate` SO 取代；**死代码 Avatar 配置链已删除**（`AvatarBaseRow/Table` + `ConfigDatabase.Avatars/GetAvatar` + 导表 `ParseAvatarBaseRow` + `Avatar_Base_Config.json/.csv`）。
  - [ ] `SpiritItem / 灵物` → 拆分为 `EffectModule / ModifierModule / LegacyMaterial`（**待定**：`ItemInRun` 仍被 `StoryEventService` 事件奖励引用，需先重构事件奖励层）。
  - [ ] `Skill / 功法` → `SkillModule / 技能模块`（**待定**：`SkillBase` 仍被 `SkillTuning` 引用，属活系统）。
  - [ ] `Talent / 天赋` → `ModuleMastery / 模块熟练度`（**待定**：随 P1 模块熟练度系统一并做）。
- [x] 检查玩家可见 UI 文案，去除已删系统旧表达（**化身 / 御灵 / 五系**）：`CharacterSelectUITK` 副标题、`法修` 角色档案 `roleTag/description`（御灵→法修/远程）已改；`化身` 残留仅存于代码注释。
  - 注：`灵物 / 功法` 为仍在用的世界观词、`修仙 / 秘境` 为已确认保留的品牌词，**均不动**（见 CHANGELOG V0.1.14「品牌层保留」决策）。

## P1 · 模块化技能系统

- [ ] 建立模块数据模型：
  - `TriggerModule`：纯条件触发器 / 状态型触发器。
  - `EffectModule`：伤害、治疗、护盾、位移、召唤、控制等执行效果。
  - `ModifierModule`：范围、次数、连锁、目标、元素、执行方式等行为改造。
- [~] 状态型触发器只提供无伤害状态展示（`TriggerType` 枚举已含四种，`TriggerTracker` 逐步接线）：
  - [x] `种子生成`：核心技能/普攻命中处种下无伤害种子（落点/存续 8s/上限 16/近距合并刷新），世界标记球可视化（`SeedSystem` + `T_种子` · Stacks 消费）。
  - [x] `种子引爆`：场上有种子即 Proc，消费时在每颗种子位置对范围敌人触发接入效果器的伤害/元素/状态并清空（`SeedSystem.DetonateAll` + `PlayerCombat.EndEnhancement` 钩子 + `T_引爆` · Single）。种子本身无伤害。
  - [x] `背击标记`：从敌人背后（背弧）命中核心技能/普攻时 Proc（`TriggerTracker.IsBackstab` 判定 · `T_背击` 模块 · Single 消费）。注：敌人常转身面向玩家，背击窗口主要出现在敌人攻击前摇/锁定他目标时（高技巧向）。
  - [ ] `傀儡计数`：记录傀儡对象或攻击次数（**阻塞·延后**：当前无持久召唤物——技能召唤仅为临时协程（`CastSummonSkill`/`DebugSummonCoroutine`），模块链 `SummonPuppet/SummonTurret` 效果器尚未 spawn，也无召唤物注册表可计数。需先做召唤/随从系统，再接 `PuppetCount`）。
  - 附带修复：`SkillHitCount`（技能命中 N 次）此前声明但未接线，已补 `SkillHitConnected` 订阅。
- [ ] 支持两种执行模式：
  - 被动：条件满足后自动触发。
  - 主动：条件满足后点亮 Q/E/R，玩家手动释放。
- [ ] 从旧技能与化身控制器中回收素材：
  - 青囊种子 → `种子生成 / 种子引爆`。
  - 影刃位移 / 背击 → `闪避后 / 背击标记`。
  - 御物傀儡 → `傀儡计数 / 傀儡协同`。
  - 剑魄完美时机 → 可回收为通用时机触发器，不再作为职业。

## P1 · 起始模板

- [~] 设计首批起始模板（现共 7 款：V0.1.13 的 3 款 + V0.1.16 新增 4 款原型；`Resources/StartTemplates/`，选择 UI 自动收录）：
  - [x] **播种者**：`T_种子 + E_毒雾 + T_引爆 + E_范围爆炸 + M_扩散`（种下→引爆循环）。
  - [x] **刺客**：`T_背击 + E_突刺 + T_闪避后 + E_飞弹 + M_连锁`（背击强化 + 闪避追击）。
  - [ ] **指挥者**：`傀儡计数 + 傀儡协同 + 护盾/分流`（**阻塞**：依赖召唤系统，随 `傀儡计数` 延后）。
  - [~] **雷行者**：`移动 X 米 + 闪避 + 落雷/连锁`（暂由既有「游侠·投射连锁」覆盖近似定位，未单列）。
  - [x] **守卫者**：`T_受击时 + E_冲击波 + E_护盾 + M_击退`（稳守反打）。
  - [x] **炼金师**：`T_每3秒 + E_治疗 + T_每5秒 + E_范围爆炸 + M_灼烧`（周期节律续航）。
  - 既有 3 款：剑修·近战爆发 / 法修·元素范围 / 游侠·投射连锁。
- [x] 旧“职业选择 UI”改为“起始模板选择 UI”（`StartTemplateSelectUI`，`flexWrap` 网格，动态收录 `Registry.All`）。
- [x] 起始模板只决定开局模块，不限制局内掉落和转向（新 4 款复用同一角色档案/核心技，仅 `startingModules` 不同；局内掉落不受模板硬锁）。
- [x] 掉落权重根据当前模板和本局已成型模块动态调整，但不能硬锁池子（`ModuleDropWeighting.PickWeighted`：基础权重恒 >0 + 模板风格 ×2.5 + 半成型链补齐 + 构筑协同；`BattleRoom.SpawnModuleReward` 接入。实测 播种者 选中时同风格 ~4.4–5.1% vs 均匀 2.2%，中性件仍 ~2.0% 不锁池）。

## P1 · 局外结算与模块熟练度

- [ ] 统一经验值结算池：
  - 死亡：0.5x。
  - 阶段返回：1.0x。
  - 通关：2.0x。
  - 完美通关：3.0x。
- [ ] 来源标签只作为经验转化提示：
  - 战斗 / 模块 / 触发器流派 / 探索 / 情报 / Boss。
- [ ] 模块熟练度替代天赋树：
  - 触发器熟练度：降低条件、强化状态上限 / 持续时间 / 可见性、解锁变体。
  - 效果模块升级：解锁新的技能效果、目标逻辑或质变效果。
  - 改造模块升级：强化技能的形态变化，例如范围、次数、连锁或延迟。
  - 起始模板熟练度：开局可替换模块、携带更多初始模块或提高推荐掉落权重。
- [ ] 旧 GrowthUITK 可回收为模块熟练度 UI，但文案和数据结构需重命名。

## P2 · 内容扩展

- [~] 首批模块池目标（V0.1.17 扩容；生成器 `PoolExpansionGenerator`）：
  - [x] 纯条件触发器 8-12 个（现 10 个）。
  - [~] 状态型触发器 4-6 个（现 3：种子生成/引爆/背击；第 4 个 `傀儡计数` 阻塞于召唤系统，其余需新增状态机制类型）。
  - [x] 效果器 20+ 个（10 → **20**；新增 减速/眩晕/易伤/无敌/净化/火雨/冰锥/剧毒/震荡波/落石，均用 PlayerCombat 已实现的 EffectType）。
  - [x] 改造件 20+ 个（18 → **21**；新增 附雷/破绽/延冷）。
- [x] 模块池按 3 大类补齐（触发器 13 / 效果器 20 / 改造件 21 / 万能 5）。
- [ ] 评审关卡特殊机制「时限采集与追猎者」：明确材料目标、传送门规则、超时追猎者/专属 Boss 强度与奖励；详见 [创意收集箱](ideas/创意收集箱.md#时限采集与追猎者)。
- [ ] 协同系统按新模块逻辑重写：
  - 普通模块链：`1 触发器 + 1 效果器 + 0-2 改造件`。
  - 隐藏组合：普通链满足额外标签、行为或状态条件后触发质变。
- [ ] 自适应怪物生态：
  - 远程 Build 过强时，部分敌人获得闪避投射物行为。
  - 召唤 Build 过强时，敌人出现清召唤 / 突脸本体行为。
  - 高移速 Build 过强时，敌人出现区域封锁或预判攻击。

## P3 · 长线与叙事

> **triage（2026-07-02）**：P3 属长线/叙事，多数依赖尚未开工的局外/元系统或明确排到 Demo3+，本阶段不实现，仅定状态。

- [~] 前哨站作为“局外配置、局内消耗”的轻量系统保留（**延后**：属局外/元系统，与已暂缓的「局外结算与模块熟练度」同批，待局外线启动后一起做）。
- [~] 寿命 / 重来机制只作为长局压力和局外叙事资源，不再与硬性搜打撤惩罚绑定（**延后**：依赖局外线；当前搜打撤惩罚已在去修仙重构中移除，无硬绑定残留）。
- [ ] 多结局、记忆碎片、叙事事件等放到 Demo3+（**明确排期 Demo3+**，本 Demo 不做）。
- [x] 旧御灵五系、化身设定只作为历史素材库，不再约束玩法（**已达成**：御灵/化身/五系系统已在 V0.1.14 去修仙重构中移除或归档，仅存历史文档；玩法不再受其约束）。

---

## 需要 Playtest 验证

- [ ] 没有职业后，起始模板是否仍能给玩家足够的身份感和开局方向感。
- [ ] 状态型触发器只提供状态载体后，起始模板是否仍然足够可玩。
- [ ] 模块熟练度是否能替代天赋树带来的长期目标。
- [ ] 死亡也结算经验后，阶段返回是否仍有存在感。
- [ ] 触发器 / 效果器 / 改造件三层组合是否易懂，是否需要更强 UI 引导。
