# 🗒️ 《仙途秘境》开发待办（Backlog）

> **用途**：跟踪"做了哪些 / 还差哪些"。本文件聚焦**剩余任务**；**已完成的详细改动**记在 [CHANGELOG](../CHANGELOG.md)。
> **配套**：[Demo 路线图](Demo路线图.md) · [主 GDD](GDD_仙途梦境.md)。完成一项就勾掉并在 CHANGELOG 追加。

---

## ✅ 已完成（速览 · 详见 [CHANGELOG](../CHANGELOG.md)）

- **v0.5.5**：本体境界/境界压制/渡劫战/转世落地；秘境异象（替代命格）；道心·因果·寿元局内效果 + HUD；链式机缘；灵脉道具掉落；掉落物乱码/朝向修复 + 拾取系统重构；菜单/术语改名；战斗配表（化身/技能）新增。
- **v0.5.4**：去"梦境"框架、改名《仙途秘境》、术语替换、死代码清理（境界突破3选1/顿悟/灵气浓度/丹药meta/法宝）。
- **v0.5**：搜打撤循环 + 洞府模块 + 修仙战斗系统。

## 🧰 范围开关现状（V.03 + 06-01 调整）

- **灵物整套**：`FeatureFlags.EnableSpiritItems = true`（v0.6.2 已打开，全套恢复）。
- **洞府 meta（闭关/灵脉/机缘）**：`FeatureFlags.EnableCaveMeta = true`（06-01「常规打开」自身进阶线）。
- 开关位 `GameConfig`（`启用灵物系统` / `启用洞府meta`）+ DebugConsole「V.03 范围开关」。

## 🔴 P1 · 完成洞府 meta 闭环（剩余）

- [ ] **天赋树渐进解锁**（GDD 9.1.7）：初始 1 基础分支 + 隐藏分支由机缘/剧情/成就解锁
  - **依赖**：天赋树具体路径**待策划填表**（现天赋数据稀疏、无分支结构）
  - 现状：悟道蒲团直接解锁所有天赋；高级机缘暂以"大量悟性"代偿"天赋分支解锁令"
  - 待填表后补：`SaveData.unlockedTalentBranches` + 分支 gating + 机缘产"解锁令"
- [x] **链式机缘**（GDD 9.1.10）✅ 2026-06-01：跨局连锁已落地
  - `SaveData`：`caveReturnCount` + `opportunityFlags` + `pendingOpportunities`（`OpportunityChainEntry`）
  - `CaveOpportunitySystem`：回府优先结算到期回访（`PopDueFollowup`）→ 否则随机机缘；`StartChain` 埋点（flag 去重）；`GetFollowup` 回访池（不入随机池）
  - 首批 2 条链：**赠予灵药→3 局后「故人来谢」**（悟性+250 / 道心+灵脉）；**接纳剑灵→2 局后「剑灵认主」**（悟性+灵脉 / 修为+因果）
  - 调试：DebugConsole「🔗 推进链式机缘（+1 回府）」
  - **后续可扩**：更多链、条件分支（用 `opportunityFlags`）、与天赋分支解锁挂钩

## 🟡 P2 · 内容 / 数值 pass

- [~] **战斗配表（化身/技能）· B 方案：表作数据层**
  - [x] ✅ 2026-06-02 CSV + 导表 + `ConfigDatabase` 读取（`GetAvatar`/`GetSkillBase`/`GetSkillEffect` + DebugConsole「📋 配表自检」）。
  - [x] ✅ 2026-06-03 **化身显示名表覆盖**：`SpiritRootRegistry` 惰性用 `Avatar_Base_Config.Name_CN` 覆盖 `name`（表 ID=(int)SpiritRootType，已修正 CSV 对齐：5 行含火/真实控制器类名）。机制仍在代码（控制器全挂载+门控，无需反射）。
  - [x] ✅ 2026-06-03 **技能 CD + 伤害 表覆盖（已跑起来）**：
    - `SkillData.configId` + `SkillTuning.EffectiveCooldown`（CD 覆盖）& `EffectiveBaseDamage`（伤害覆盖）；`PlayerCombat` CD 三处 + 伤害三处(Area/Projectile/Dash trail)已接入。
    - **12 个技能 SO 已批量填 `configId`(1~12)** + `Skill_Base_Config.csv` 对齐真实 12 技能（CD=现值、BaseDamageRatio=10000 中性，不改平衡）。
    - **伤害规则**：`BaseDamageRatio` = 对 SO 基础伤害的**百分比乘区**(10000=100%)；与 UpgradeRoom 就地升级**共存**（升级抬基数、表按%微调），不写回 SO。
    - 现在改 CSV 的 CD/百分比 → 导表 → 即时生效。
  - [x] ✅ 2026-06-05 **表重做**：策划新表 19 技能 + `Rarity` 列；自设计**业火专属 #20 焚天·业火燎原**入表；化身 `Exclusive` 列（剑魄16/青囊17/影刃18/御物19/业火20）。
  - [x] ✅ 2026-06-05 **取消表→SO 自动同步**：按决定删除 `SkillConfigSync`；今后新增技能/功能由用户提出再手动落地。
  - [x] ✅ 2026-06-05 **SO 实体化 + 池接入（已用 Unity MCP 直接建好）**：在 `Assets/1Game/Data/Skills` 建/更新 11 个 SO（烈焰斩/御风诀/寒冰封印/混沌吞噬/土遁术(更新)/缩地成寸/御风术/天罡北斗阵/九天玄火阵/冥河召唤/轮回一击），各填 configId + 机制字段，并接入 `Demo1Setup.skillPool`（12→22）、保存 Demo1 场景。
  - [x] ✅ 2026-06-09 **遗留旧技能 SO 重映射**（雷锁链/寒冰诀/天雷引/镜花水月/影步/烈焰掌/傀儡术/回春术/御剑术）：已在 `Skill_Base_Config` 追加 ID 21-29，并通过 MCP 将 9 个 SO 的 `configId` 分别设为 21-29。BaseDamageRatio=10000（100%保留 SO 原伤害）；运行时 `SkillTuning` 读表覆盖 CD/伤害。
  - [ ] **余下**：化身描述/`DefaultItem_ID`/`Restriction`（灵物已解封 v0.6.2，可填表）；`Skill_Effect_Config` 需通用效果解释器才能驱动。

- [~] **技能机制实现路线（全 20 技能·分批）** — 机制代码就位后，**SO 创建+接 `skillPool` 为 Unity 手动步骤**（SO 配方见 [CHANGELOG v0.5.7](../CHANGELOG.md)）
  - [x] ✅ **① 增益类**：补齐 `SkillType.Buff` 调度死分支；`CastBuffSkill` 泛化（攻速/移速/攻击/减伤，经 `StatusEffectController` 计时）。覆盖 御风诀/御风术/金钟罩。
  - [x] ✅ **③ 区域类**：通用 `ActiveSkillZone`（伤害/减速/黑洞吸引/灼烧/随身）+ `SkillType.Zone` + `CastZoneSkill`；`EnemyBase.ApplySlow`。一次覆盖 混沌吞噬/天罡北斗阵/九天玄火阵/冥河召唤。
  - [~] **④ 新奇单体**：
    - [x] ✅ 寒冰封印（`freezeOnHitChance/Duration` 命中冻结）
    - [x] ✅ 土遁术（`dashInvulnerable` + `PlayerController.SetInvincible` 钻地无敌）
    - [x] ✅ 轮回一击（`RunCombatStats` 累计本局伤害；`damageFromRunTotal` 按比例结算）
    - [x] ✅ 水镜术：`WaterMirrorDecoy` 嘲讽分身（`EnemyBase` 读 `ActiveTransform` 重定向）
    - [x] ✅ 金蝉脱壳：`LethalGuard`（Buff 武装；`PlayerController.OnDamage` 致命前拦截→免死+替身爆炸+瞬移）
    - [x] ✅ 天地大挪移：`HeavenEarthShift`（受伤反弹+免疫；普攻转治疗 10s）
    - [x] ✅ **SO 全部建好+接池（MCP）**：水镜术(7)/金蝉脱壳(9)/天地大挪移(15)，skillPool 25
  - [x] ✅ **② 基础补 SO**：烈焰斩/缩地成寸（MCP 已建）。
  - [x] ✅ **⑤ 化身专属(16-20)**：`SkillType.AvatarSpecial` + `avatarSpecial` 路由（按当前化身门控）；
    - 一念刹那(16)→`SpiritRootGoldController.UnleashOneThought`（剑心通明强化斩）
    - 枯荣逆旅(17)→`SpiritRootWoodController.DetonateSeeds`（引爆周围种子；99层被动后补）
    - 息影瞬步(18)→`SpiritRootWaterController.EnterShadowStep` + `PlayerController` 闪避免充能 + 路径伤害（5s）
    - 兵阵合一(19)→`SpiritRootEarthController.TogglePuppetArrayMode` + `EarthPuppetTurret`（5 炮台 AOE；再次释放撤阵）
    - 焚天·业火燎原(20)→`SpiritRootFireController.IgniteInferno`（强化狂火+结束引爆）
    - **SO 全部建好+接池（MCP）**：16-20，skillPool 30。
  - [x] ✅ **专属技能掉落门控**：`SkillData.RequiredRoot` + `SkillPickup.Spawn` 单一出口——化身专属只对对应化身掉落（修复"青囊掉到影刃专属、放了没效果"）。
  - [x] ✅ **保底奖励重抽（2026-06-09）**：`SkillPickup.PickValid` 先过滤掉其他化身专属再随机，所有掉落点（BattleRoom + 5 种敌人 TryDropSkill）统一调用，不再出现"通关了但什么都没掉"。
  - [ ] **枯荣逆旅被动**：99 层上限 / 攻击不消耗层数 / 每层 +0.1% 需常驻装备被动机制（当前仅主动引爆）。

- [x] ✅ **打包调试支持 / ~~道伤可视化~~（2026-06-05）**（注：道伤系统已于 v0.6 移除）
  - 打包 bug 修复：山门按 F 无反应（历史原因为道伤锁死，道伤已移除）。
  - `DebugConsole`：📜 日志面板（捕获 `Application.logMessageReceived`，打包可看 log）/ 🗡 发当前化身专属技能；精简掉灵物升满·灵物爆率·链式机缘推进·掉落灵脉道具等无用按钮。

- [ ] **技能系统·后续打磨（全 20 机制已落地，以下为非阻塞）**
  - **数值/手感**：表 16/17/19 的 `BaseCooldown` 为占位（0/0/1），按手感在 `Skill_Base_Config` 调；混沌吞噬吸引力度、冥河减速%、业火引爆倍率、轮回一击 10% 系数等需 playtest 校准。
  - **被动补全**：枯荣逆旅「种子上限 99 / 攻击不消耗层数 / 每层 +0.1% 伤害」需常驻"装备被动"机制（当前仅做了主动引爆）；金蝉脱壳设计为"受致命自动触发"，当前实现为主动武装一段时间（需被动技能框架才能做到完全被动）。
  - **化身门控 UI**：AvatarSpecial 现按当前化身静默门控（非该化身放了打印灰字）；后续在拾取/技能栏标注"专属"并禁止非本化身装备。
  - **视觉打磨**（见下"视觉现状"）：持续状态缺常驻指示（天地大挪移/息影瞬步无身上光环）；Zone/黑洞缺持续粒子（现为褪色 cube）；轮回一击缺蓄力条与"累计伤害"读数。
  - **playtest 验证**：黑洞吸引 / 冥河大幅减速 / 业火结束引爆 / 金蝉免死脱身 / 天地大挪移反弹+治疗 / 息影无 CD 闪避 / 兵阵炮台锁敌 / 一念剑心翻倍 / 水镜嘲讽是否真的拉走仇恨。

- [ ] **技能视觉现状（占位级，`showDebugVisuals=true` 默认开）**
  - ✅ 已有占位 VFX：AreaDamage/Dash 元素爆发(`FxFactory`)、冻结(敌人变蓝+飘字)、Zone 地面 cube、黑洞/阵随身 cube、水镜半透明胶囊、金蝉爆炸 cube、土傀儡方碑+炮击爆发、剑斩 SliceLine、业火元素爆发+复用狂火特效、息影闪避 SliceLine、各类飘字标签。
  - ⚠️ 仍是"程序占位"（primitive/cube/圆环），非美术资源；持续状态缺常驻光环；Zone 缺循环粒子。正式 VFX 接 `vfxPrefab` 后可逐个替换（SkillData 已留 `vfxPrefab` 字段）。

- [x] ✅ **回灵丹/灵藤草/聚气丹归类 + 协同重挂（2026-06-09）**：回灵丹→护体、灵藤草→护体、聚气丹→异变（SO + Demo1DataCreator 同步）；`SynergySystem` 11 条 Pill 组合全部改挂 Defense/Anomaly（去重后微调阈值避免重复触发条件）；"丹元归元"→"归元护体"。`Pill` 枚举保留防序列化错位但不再被任何 SO 使用。
- [x] ✅ **灵脉道具入掉落池 + 差异化掉落（2026-06-02 / 06-09）**：`SpiritVeinPickup`（走近自动汲取→`InjectExp`，HUD 飘字）。Boss 50%「聚灵珠」+150 / 宝藏房 60%「灵脉碎片」+50 / **深层(≥3层)宝藏房 40%「地脉精华」+100** / **渡劫突破奖励「洞天残核」+200**。
- [~] **灵脉 `ModuleEfficiency` 接入洞府模块**：✅ 灵田生长速度已接（`growDuration / ModuleEfficiency`，面板显示×倍率）；炼器/灵兽园目前是**瞬时**无计时（不适用）；若后续给二者加计时/批量产出再乘效率
- [ ] **火属性化身重设计**：v0.4 撤销后一直挂着（用渡劫/心魔/灵脉等新机制做载体）
- [ ] **灵物 17 → 更多 / 协同 10 → 更多**（GDD 目标 150+ / 100+）
- [ ] **6.5 槽位修饰扩充**（落石→陨石类）：已有框架，扩内容

## 🟢 P3 · 叙事 / 长线（Demo3 范畴）

- [~] **道心 / 因果完整玩法**：`PlayerStateHooks` 仍是数值源
  - [x] ✅ 2026-06-02 **阈值局内效果**（`MoralEffects`，常驻挂在玩家上）：道心 入定 +攻击 / 心摇 +受伤 / 入魔 高伤高险；因果 善缘减伤 / 业障·重业 加受伤。调试：DebugConsole【道心/因果】区
  - [x] ✅ 2026-06-02 **道心挂钩渡劫成色**（`TribulationTrial.ResolveQuality`）：受创基础档 + 道心修正档（入定+1/清明0/心摇-1/入魔-2，钳制 0~3）；渡劫 HUD 实时显示道心及成色加成
  - [~] **命格玩法**：~~命格 combat 被动~~ 已退役（存在感低）→ 改为地图侧「**秘境异象**」系统（见下"已完成"）。`RollFate` 保留写 `fate_xxx` BossFlag 供事件条件
  - [x] ✅ 2026-06-02 **寿元玩法**（`MoralEffects` 低寿元衰朽减益 + 机缘「古修遗宝」折寿换修为）：调试「⏳ 寿元 ±25」。余：寿元长线/自然流逝
  - [x] ✅ 2026-06-02 **修仙状态 HUD**（`RunHUD.DrawMoralStatus`，右上角）：道心(状态+值)、因果债(≠0时)、寿元(≠100时) 可视化
  - [ ] 余下：道心/因果/命格专属事件选项与 Boss 联动（待事件填表）；命格本身对玩家的显性提示（现隐藏，仅事件条件用）
- [ ] **多结局**：飞升 / 入魔 / 凡途 / 真道（道心/因果/缘分驱动）
- [ ] **世界观叙事 + 记忆碎片**
- [ ] **秘术系统**（8 种）`⏸️ Demo3+ 暂缓`：残念护身 / 元婴二命 / 道身分离 / 轮回残影 / 因果轮转 / 天劫淬体 / 心魔具现 / 时光倒流
  - ⚠️ 部分效果不合理 / 引用已删系统（炼丹房等）/ 与转世归零冲突 → **实现前需重评**（详见 GDD 9.2 注）

---

## 🔧 技术债 / 改名遗留

- [x] **掉落物拾取系统重构** ✅ 2026-06-02（分两步均完成）：
  - 抽 `PickupBase`（公共：浮动旋转 / 触发器 / InteractionRouter / 长按分解 / 提示显隐生命周期）+ `WorldPromptPanel`（共用世界提示面板构建，`BillboardUI` 也搬入）。
  - `ItemPickup` / `SkillPickup` 改为 `PickupBase` 子类，仅各自实现"视觉/提示内容/主操作/分解/换槽模态"；~90% 重复已消除，提示风格统一、一处改全局生效。
  - 严格保留原交互：F 轻按/长按、洞府素材 vs 灵物分叉、对象池回收、功法满槽 Q/E/R 换槽模态、灵物开关（v0.6.2 默认开启）。
  - 两文件**注释乱码**随重写一并清干净（运行时字符串此前已修）。
  - 注：`SpiritVeinPickup`（自动吸收·无提示框）交互模型不同，未纳入基类（无重复）。
  - ⚠️ **需 playtest**：拾取/装备/分解长按、功法满槽换槽 Q/E/R/Esc、多拾取物重叠时 F 路由、提示朝向/缩放。

- [x] ✅ `ItemCategory.Pill` 枚举彻底删除（2026-06-09）：所有枚举项加显式编号（跳过 4），Pill 移除；CodexUITK/ConfigDashboard/ItemInventory/QualitativeEffectRunner 等 6 处引用全部清理。30 个 SO 序列化验证正确无错位。
- [x] CodexUI 残留空"丹药"分类 chip ✅ 2026-06-01：图鉴不再绘制 Pill 分类 chip（保留数组下标维持与枚举对齐）
- [x] `VillagePortal` "魂伤 / 入梦"旧术语 ✅ 2026-06-01：已改新术语；并连带清理**全部玩家可见**旧词（MainMenu 标题/按钮、RunHUD 道伤、洞府模块"下次入秘境"、炼器"秘境掉落池"、死亡/通关面板按钮等）
  - 备注：内部 Debug.Log / 代码注释里的"入梦/梦境/残魂"仍保留（不影响玩家，低价值，未动）
- [x] Unity 编辑器菜单路径旧名 `仙途梦境/...` ✅ 2026-06-01：代码 `MenuItem` / `CreateAssetMenu` / `ExecuteMenuItem` 全部改为 `仙途秘境/...`（含 ToolSearchWindow / ConfigDashboard / Demo1DataCreator 等交叉引用一致更新）；相关 Docs 菜单引用同步
- [ ] 文档文件名仍为 `GDD_仙途梦境.md` 等（**有意保留**以免断链；如要改需全局更新引用）

---

## 🔮 待设计确认后落地

- [ ] **特殊事件配表**（问题5）：需用户先出事件设计稿（如叶修之死），再建 `Event_Config.csv`（ID/触发条件/选项/效果）+ 通用事件解释器。
- [ ] **御灵成长天赋树**（问题6）：推荐"五系专属分支 + 通用主干"，需用户确认方向后建 `GrowthTree_Config.csv` + UITK 树形 UI。

## 🧪 需 playtest 验证的点

- [ ] **渡劫战在洞府场景的战斗可用性**：心魔镜像在洞府能否正常被攻击/反击（用的是秘境同款 `InnerDemonMirror`）
- [ ] 整条洞府 meta 串跑：入秘境攒历练值 → 撤离 → 闭关/灵脉分配 → 机缘 → 冲击境界渡劫 → 境界压制下探更深
- [ ] 心魔乱入：因果/道心抉择（机缘）攒满心魔值 + Boss 战 → 乱入
- [ ] **v0.6.2 新增验证**：撤离成果面板弹窗 + 层深倍率；buff tooltip 悬停；灵物掉落全链路；洞府残留清理；化身卡片角色标签；闭关成色加成

> 调试入口（DebugConsole）：
> - 【本体境界】修为+200 / 历练值存量+200 / 灵脉经验+200 / 触发机缘 / 🔗推进链式机缘 / 心魔值+50
> - 【道心/因果】道心±25 / 因果±12 / 寿元±25 / 💎掉落灵脉道具
> - 【秘境异象】雷泽 / 灵潮汹涌 / 心魔滋生 / 万灵复苏 / 血月 / 道心试炼 / 因果轮回 / 机缘频现 / 清除
> - 【V.03 范围开关】灵物系统 开/关 · 洞府meta 开/关
