# ProjectR CHANGELOG

> 仅记录 2026-09-02 ProjectR 产品重置后的有效变更，最新在上。旧产品历史通过版本控制追溯。

## P3.90 初契对比提示与遭遇演出收口（程序＋策划，2026-09-16）

- 保持“任意3种组合即可完成”的宽松目标，同时在首个组合后依次弱提示“同宠换动作”和“同动作换宠”；提示不锁按钮、不阻止其他组合完成。
- “选好了”的第二次确认改为明确展示永久结契灵宠、保留赋予动作及其效果；确认成功后显示“初契完成”里程碑，目标切为“进入前方空地，保护照料员”。
- 第一波增加0.9秒草丛动静与两处出生预警；两只Monkey仍计为同一波，第二只延后0.8秒现身，避免同时贴脸开打。
- Monkey退开后由远端刃铠灵先威吓再接管正式战斗体；入场增加火系落点反馈。凶势归零后先回望0.35秒，再于2.1秒内后撤10米并以扬尘／风痕结束。
- 运行验证第一波注册2只时先激活1只、延迟后2只均激活，击退后刃铠灵正确接管且预告体退场；Unity编译与运行控制台0错误，全量167项ProjectR合同测试通过。

## P3.89 初契收口与第一波生成修复（程序＋场景＋策划，2026-09-16）

- 赋予试玩达到`3 / 3`后的主按钮改为固定“选好了”，二次点击阶段收敛为“再次点击确认”，不再在按钮上重复灵宠名和动作名。
- 采用本轮人工认可的镜头构图并整数化：序章跟随偏移改为`(0, 18, -10)`、俯角`63°`、垂直FOV `60°`。
- 删除结契后、第一波前重复要求`LMB / Q / SPACE`的基础操作门槛；赋予试玩已经验证动作，玩家进入遭遇区即生成两只躁动Monkey，实战中仍保留一次当前附着显化提示。
- 修复HIGHLANDS场景恢复时遗失三个战斗出生标记的问题，在战斗区NavMesh补回两处近战点和一处刃铠灵点。运行验证进入遭遇后`running=True / remaining=2`，两只Monkey即时生成；Unity编译0错误，全量167项ProjectR合同测试通过。

## P3.88 响响鸮身法回声起点修复（程序＋策划，2026-09-16）

- 修复响响鸮附着SPACE的实现与说明不一致：`DodgeFinished`现同时携带闪避起点和终点，Legacy与载体运行时两条闪避路径均正确发布起点。
- 单宠显化按种族选择位置：响响鸮在闪避起点留下延迟回声；火花狸与弹弹胶继续在闪避终点产生火花或弹力冲击。
- PlayMode验证闪避事件保存独立起终点；三宠位置合同验证通过，Unity编译0错误，全量167项ProjectR合同测试通过。

## P3.87 试玩组合独立命中计数（程序＋策划，2026-09-16）

- 修复LMB试玩进度跨动作保留造成的误判：同一灵宠先命中2次、切到Q再切回LMB时，旧逻辑会让下一次命中直接触发“第3击”效果。
- 常驻教学每次切换灵宠或动作都重建临时试玩运行态，使每个组合从0开始计数；只影响初契试玩，不改变正式战斗的安全处换挂规则。
- PlayMode验证`2次LMB→切Q→切回LMB`后进度归零，下一次命中仅推进至`1 / 3`且不显化；Unity编译0错误，全量166项ProjectR合同测试通过。

## P3.86 教学前危机铺垫与单宠显化位（程序＋策划，2026-09-16）

- 教学前对话扩为“照料员喝止→刃铠灵威吓回应→主角确认伤势→照料员说明腿伤、驱兽火将熄和其他威胁靠近→三宠回应→催促赋予”的完整危机节拍。
- 刃铠灵预告体新增0.9秒威吓信号：武器高亮增强、快速警戒摆动、体型短促放大并生成火系落点反馈；对话镜头在照料员与远端威胁之间切换两次。
- 三只幼宠仍在剧情中依次现身；进入赋予面板后统一收至一个专用显化位，未选择时全部隐藏，之后只显示当前选择的一只，避免三只模型挤在照料员旁边。
- Yarn新增`CaretakerInjured`节点并扩写`CaretakerWarning／CandidatesRespond`；PlayMode验证专用显化位只显示当前灵宠、威吓信号可运行，Unity编译0错误，全量165项ProjectR合同测试通过。

## P3.85 常驻赋予单击即换（程序＋策划，2026-09-15）

- 修复自由试玩中卡片金色高亮代表“预览”、实际仍沿用上一组合的认知错误；该状态会让响响鸮看似选中但继续触发弹弹胶弹射。
- 首次聚光教学完成后，点击任意灵宠或动作立即应用对应组合；自由阶段移除“应用当前组合”按钮和待应用状态，面板只高亮并描述当前真实生效组合。
- PlayMode验证弹弹胶Q切到响响鸮只需单击一次，控制器与运行态均立即变为响响鸮Q，显化不生成弹弹胶追踪弹体；Unity编译0错误，全量165项合同测试通过。

## P3.84 常驻赋予面板与点击聚光引导（程序＋策划，2026-09-15）

- 赋予教学由“同宠换动作→同动作换宠”的单变量考试改为简单目标：任意三种不同“灵宠＋动作”组合各实际触发一次显化即可完成。
- 首次赋予改为三拍强引导：背景压暗，只允许并高亮当前应点击的灵宠卡、动作卡或“应用组合”按钮。
- 首次组合应用后面板保持常驻，三宠与`LMB / Q / SPACE`可随时预览、应用和切换；已实际使用的组合显示勾选，进度达到`3 / 3`后开放结契。
- PlayMode验证首次赋予后立即进入常驻面板、三种不同组合正确计数、全部六个选择按钮持续可用并在完成后解锁结契；Unity编译0错误，全量165项ProjectR合同测试通过。

## P3.83 弹弹胶Q弹射目标修复（程序，2026-09-15）

- 修复基础灵息弹命中敌人子碰撞体时，弹弹胶Q显化无法排除刚命中敌人根对象的问题；载体结算、命中配对和弹射搜索现统一归一到`IDamageable`根对象。
- 原地重复命中导致“只见爆炸、不见弹射体”的路径已消除；PlayMode以子碰撞体首目标和相邻第二目标验证Q显化会创建追踪弹射体。
- Unity编译0错误，全量165项ProjectR合同测试通过。

## P3.82 赋予教学组合软预览（程序＋策划，2026-09-15）

- 修复第2步无法切换灵宠、第3步无法切换动作而看不到其他组合说明的问题：六个选择按钮在三个教学步骤中始终可用，点击只更新预览，不立即改变当前附着。
- 单变量教学由硬锁改为确认校验：第2步可浏览全部组合，但只有“当前灵宠＋不同动作”可确认；第3步同样可浏览全部组合，但只有“不同灵宠＋当前动作”可确认。
- HUD明确显示“刚才／预览”组合与本步需要保持的变量；不符合目标时确认按钮保持禁用，避免浏览操作破坏教学顺序。
- PlayMode验证第2、3步可自由浏览灵宠和动作、组合说明同步更新、合法组合才可确认；Unity编译0错误，全量164项ProjectR合同测试通过。

## P3.81 赋予灵宠教学改版（程序＋策划，2026-09-15）

- 取消P3.79“三只灵宠由系统连续附着LMB并逐只试玩”的策划目标；教学重点改为让玩家亲手学会赋予操作。
- 必修采用单变量对比链：玩家任选`灵宠P1＋动作A1`完成首次赋予；保持P1、任选不同动作A2完成同宠换动作；保持A2、任选另一只灵宠P2完成同动作换宠。三次都必须由玩家确认并在木靶真实触发显化。
- 三步完成后开放三宠×`LMB / Q / SPACE`自由试玩和随时结契，不强制第三只灵宠或完成九种组合；最后一次确认位置直接作为救援战初始附着，避免结契后重复选择。
- `StarterSpiritTrialController`已替换为九态引导流程，不再自动选宠或换位；每次选择先作为预览，玩家点击附着／改附／替换后才改变临时运行态。8／15／25／45秒渐进提示只加强当前操作，45秒后仍要求玩家亲手执行下一次有效动作。
- 自由试玩继续使用“选择组合→应用组合”，结契原子提交当前灵宠与最后附着位置并直接推进到`AttachmentChosen`，不再弹出重复首次附着界面。
- Unity编译0错误；PlayMode完成任意`P1＋A1→P1＋A2→P2＋A2`、锁定单变量、自由试玩预览／应用验证，全量163项ProjectR合同测试通过。

## P3.80 HIGHLANDS Demo 1序章恢复（场景，2026-09-15）

- 确认序章视觉源是`HIGHLANDS - Stylized Environment/Demo/Demo 1.unity`，而非运行时村庄测试场景`Assets/1Game/Scenes/Demo1.unity`；从保留手工布置的源场景重新生成正式`StarterPrologue.unity`。
- 恢复五组中文层级、最新V5 Guide、运行时系统、玩法标记与交互，重新执行战斗区树石／高草清理、三块边缘掩体构图和出生点净空，并接回三只帕鲁、Monkey与刃铠灵Prefab。
- 重新预烘序章NavMesh：559顶点，战斗入口、两只Monkey、刃铠灵和回家点均可采样；PlayMode确认玩家、Bootstrap、唯一活动选择触发器和NavMesh正常。
- 正式场景已显式保存，旧白盒场景备份与临时重建场景已删除；Unity编译0错误，场景缺失引用0项。

## P3.79 三宠打靶试玩与自由结契（程序＋策划，2026-09-15）

- 初契永久选择前新增真实试玩流程：火花狸、响响鸮、弹弹胶依次临时附着到LMB，玩家必须分别在两个不死练习靶上完成3次有效命中并看到火花、延迟复响、第二目标弹射。
- 三段普攻完成后开放非暂停自由试玩面板，可切换任意候选与`LMB / Q / SPACE`；面板逐组合说明触发条件和效果、记录已显化勾选，并允许玩家随时对当前候选二次确认结契。
- 新增`StarterSpiritTrialController`、`StarterSpiritTrialHUD`与`StarterSpiritTrialTarget`；`StarterSpiritCarrierController.BeginStarterTrial`创建不落档的临时实例，普攻必修阶段不提前装备Q，进入自由试玩后才临时开放灵息弹；确认后仍复用原永久选择、正式附着、存档与候选退场合同。
- 练习靶承接真实玩家伤害事件但不死亡、不计入本局伤害；试玩中途退出不会污染契匣、出战GUID或序章检查点。
- Unity编译0错误；运行验证完成三只候选自动轮换、两个练习靶生成、自由阶段换宠／换挂和中文面板显示，ProjectR全量161项合同测试通过。

## P3.78 灵息弹替换为特效包粒子（程序，2026-09-15）

- 从现有Hovl Auras Pack 2筛选URP支持的`Glow aura`，组合为游戏专用`StarterSpiritBoltProjectile.prefab`；保留独立碰撞根节点，以4组循环粒子代替易误读为消失的蓝白程序球体，不修改共享特效包源资产。
- `StarterSpiritBolt.asset`正式引用新Prefab，最大视觉飞行时间由0.8秒提高到3秒；命中墙体、石块或敌人仍立即消失，布景命中继续产生暖金反馈。
- Prefab路径和调试回退路径统一调用初始灵息弹配置；新增`StarterSpiritBoltVisual`，对象池每次租用时清空并重播粒子，避免第二次及以后施法没有视觉。
- Unity编译0错误；合同确认Prefab包含`Projectile`、对象池粒子重播组件和4组粒子系统。

## P3.77 Debug面板收敛为计时器（程序，2026-09-15）

- Tab面板移除事件、战斗、节点直达、日志与记录点等全部运行时按钮，只保留大号秒表、“开始／暂停”和“重置”。
- 面板改为330×174紧凑布局；开始按钮会随状态切换为“暂停”，秒表显示“运行中／已暂停”。
- 创建面板时主动确保`InputSystemUIInputModule`存在，修复鼠标点击无响应。PlayMode使用真实Input System指针按下／抬起验证“开始”可点击，运行时面板仅有2个可交互按钮；Unity编译0错误。

## P3.76 Debug Playtest计时器（程序，2026-09-15）

- Tab Debug控制台新增Playtest秒表，使用不受`Time.timeScale`、暂停或Yarn对话影响的真实时间；状态区以`MM:SS.s`持续显示运行／暂停状态。
- 提供“开始／暂停”“记录当前时间”“归零”三个按钮，记录点写入Debug日志，便于手工记录初契、开战、头目撤离和转场时间。
- 序章启动时独立补建`DebugConsole`，修复干净启动`StarterPrologue`未创建`GameManager`因而Tab入口缺失的问题。
- Unity编译0错误；PlayMode确认序章生成3个计时按钮，秒表可累计、暂停并归零。

## P3.75 对话任意按钮推进修复（程序，2026-09-15）

- 修复序章Yarn面板`CanvasGroup`关闭交互和射线导致按钮点不到的问题。
- 推进输入改用Input System标准`onAnyButtonPress`：响应任意键盘键、鼠标按钮和手柄按钮，不把鼠标移动、滚轮位移或摇杆漂移当作继续；新行出现后延迟0.12秒接收输入，避免触发剧情的同一次按键误跳对白。
- 面板提示改为“任意键显示全文／任意键继续”，并加宽按钮。PlayMode验证任意A键可推进到第二句、点击界面外任意位置可结束第二句，对话结束后时间缩放恢复为1。

## P3.74 Yarn序章对话、教学说明与敌人耐久（程序＋策划，2026-09-15）

- 通过OpenUPM固定接入MIT许可的Yarn Spinner 3.2.7（当前Unity 2022.3兼容），新增独立`StarterPrologue.yarnproject`与7个剧情节点；开场、照料员警告、三宠回应及三种所选灵宠的战后对话均迁入Yarn文本资产。
- `StarterPrologueDialogueSystem`作为ProjectR到Yarn的唯一适配层；自定义Presenter复用现有uGUI/TMP风格，以每秒20字逐字显示。玩家第一次点击可补全文字、再次点击继续，Enter／Space同样可用；对话期间暂停游戏并释放鼠标，单句45秒无输入自动放行。
- 修复多行对话的完成回调竞态：每句使用独立完成句柄，第一句结束不会误跳过第二句。PlayMode确认战后两句必须分别点击，第二句正确显示实际所选“火花狸”，结束后时间缩放恢复为1。
- 基础战斗检查表为LMB、Q、SPACE补充“近身连续攻击／中距离直线攻击／快速拉开距离”用途说明；Monkey实战提示进一步说明当前灵宠触发效果。
- 两只躁动Monkey生命倍率由0.55提高到1.0，实测单只最大生命由约17提高到30；刃铠灵凶势倍率由0.9提高到1.5，伤害倍率仍保持0.25，增加教学观察和操作空间而不提高失败压力。
- Unity编译0错误；Yarn节点／Presenter／教学／数值／场景／波次共6项相关合同测试通过。

## P3.73 灵息弹恢复墙体阻挡与碰撞反馈（程序，2026-09-15）

- 按最新反馈撤回P3.72的穿透布景处理：灵息弹命中墙体、石块或敌对目标后均立即消失；关卡侧通过加大关键石块阻挡解决碰撞体过小问题。
- 序章基础灵息弹命中无伤害场景阻挡时新增0.24秒暖金色小型爆点与圆环，和蓝白飞行本体、敌对目标命中反馈区分；逻辑Trigger仍不会吞弹。
- Unity编译0错误，配置合同测试通过；PlayMode确认石块碰撞后灵息弹回收，生成反馈色`RGBA(0.85, 0.70, 0.40, 1)`。

## P3.72 序章灵息弹布景吞弹修复（程序，2026-09-15）

- 定位Q技能模型突然消失的根因：`StarterSpiritBolt`仍使用程序投射物，通用碰撞规则会在飞行约4米时命中HIGHLANDS石块并立即回收，并非主角或投射物Renderer剔除。
- 序章基础灵息弹现在忽略无伤害目标的地形与布景碰撞，仍正常命中可受伤目标；同时让程序投射物实际采用技能配置的0.8秒飞行时间，避免穿越布景后沿无限距离飞行。
- 新增碰撞／寿命合同测试；Unity编译0错误。PlayMode在原复现路径加速穿越石块与Terrain后本体仍存在，并在最大飞行时间后正常清理；验证存档已恢复干净新档。

## P3.71 序章混合式战斗详细引导（程序＋策划，2026-09-14）

- 首次附着后进入遭遇区时暂缓刷怪，以右侧非阻塞检查表要求玩家真实完成`LMB普攻 / Q灵息弹 / SPACE闪避`；三项任意顺序打勾，完成后短暂确认并生成两只躁动Monkey。30秒无操作时自动放行，教学HUD异常也不会锁住主线。
- Monkey实战根据实际首次附着只追踪一个显化目标：武器累计3次命中、Q命中敌人或在敌人附近完成身法。复用`StarterSpiritCarrierAdvanced`显示进度，首次`Activated`后永久收起；开战10秒未显化时只强化提示一次，不暂停战斗。
- 第一波清空后新增2.5秒节奏缓冲；刃铠灵入场只说明“削弱凶势，迫使它撤退”，不逐招教学。远程出生标记异常时改用战斗区NavMesh位置兜底，波次注册异常会复位遭遇并提示重新进入，避免软锁。
- 基础三动作完成状态按当前灵宠实例GUID保留于本次运行会话：同一次死亡重载不重复，重新启动并从`AttachmentChosen`进入仍可再次教学；不增加存档字段、不改schema v9。
- Unity编译0错误；新增3项纯状态合同测试并与场景／两阶段遭遇／首次附着合同共6项通过。PlayMode确认教学完成前敌人0只、完成后生成2只，武器／Q／身法三种附着均能进入`CarrierComplete`，刃铠灵阶段不含具体招式提示。

## P3.70 照料员静态受惊与小镇方向退场（程序＋策划，2026-09-14）

- 移除照料员根节点每帧左右颤抖；初到继续使用Druid倒地待机与脉冲惊叹号表达害怕，避免角色长时间机械抖动。
- `StarterPrologueCaretakerExit`不再使用固定的旧来路向量，改为从照料员当前位置指向场景`HomeReturn`标记；当前实际退场方向为`(0.929, 0, 0.370)`，与小镇／回家点方向点积为1。
- Unity编译0错误；PlayMode连续采样确认害怕状态下根位置和旋转保持不变，退场方向与固定回家点完全一致。

## P3.69 初契UI回退、照料员实装与头目收尾顺滑（程序＋策划，2026-09-14）

- 按最新体验反馈撤销P3.68轮播＋合并附着界面，恢复上一版稳定三按钮三选一、详情区和独立二次确认；初契成功后继续进入原有LMB／Q／SPACE首次附着引导。
- 照料员改用项目内KayKit `Druid`模型与专用`Fear / Calm / Run`控制器：初到时播放倒地动作、轻微颤抖并显示脉冲惊叹号，头目撤离后起身恢复，再于对话结束后沿来路退场。白盒胶囊渲染与碰撞在运行时关闭。
- 修复刃铠灵视觉断层：第一波开始时不再删除远端预告体；两只Monkey退开、正式战斗体在同一出生点生成后才移除预告体，使头目从远景警戒连续衔接到入场战斗。
- 凶势归零后镜头跟随刃铠灵向远离玩家方向奔逃8米，完成非致死撤离后立即聚焦照料员并播放两段对话；对话结束才写入`RescueCompleted`、发布固定回家点目标并让照料员退场。退场逻辑不再重新激活未选幼宠。
- Unity编译0错误；PlayMode验证Druid模型、倒地状态与惊叹号均有效，第一波开始后预告体保持、正式头目生成后才替换，击败后出现“多亏你和火花狸”战后对话；4项选择／场景／遭遇／附着合同测试通过，验证存档已恢复干净新档。

## P3.68 轮播式三选一与首次附着合并（程序＋策划，2026-09-14）

- 初契界面改为单只候选居中展示：左右箭头或A／D在火花狸、响响鸮、弹弹胶之间循环切换，始终保持“三选一”语义，不再同时铺开三张卡。
- 右侧以三个圆形动作槽展示武器普攻、Q灵息弹和SPACE身法；悬停、点击或数字键1／2／3可切换首次附着，当前槽放大高亮，并实时显示当前灵宠对应的强化效果。
- 二次确认一次提交灵宠与首次附着，成功后检查点直接推进到`AttachmentChosen`，三只场景候选按原规则退场，不再紧接着弹出独立附着界面；若运行时控制器异常，仍保留原附着HUD作为恢复兜底。
- Unity编译0错误；PlayMode验证左右切换可从火花狸更新为响响鸮、动作槽可切到SPACE并刷新“回声脉冲”说明，一次确认后UI关闭且存档写入`AttachmentChosen / Mobility`。验证后测试存档已恢复干净新档，3项相关合同测试通过；完整契约套件仍被既有天赋树Sprite销毁异常中断。

## P3.67 初到隐藏、剧情显现与三宠退场（程序＋策划，2026-09-14）

- 初到救援空地时三只候选默认关闭Renderer与选择Collider，只展示倒地惊慌的照料员；照料员占位姿势改为侧倒并降低重心。
- 玩家进入共同触发区后先聚焦照料员并播放警告，再按火花狸→响响鸮→弹弹胶顺序短促显现，随后展示远端刃铠灵并打开原有三选一UI。
- 确认结契后另外两只候选立即退场，所选灵宠完成约1.4秒附着显现后也退场；场景不再保留任何候选模型，所选结果由HUD／附着系统接管。读档处于`StarterChosen`及以后时同样隐藏全部候选。
- 三只候选根Collider在Scene和`StarterPrologueWhitebox.prefab`中默认关闭，因此不进入预烘导航源；重新烘焙NavMesh后顶点由629降至559，三只候选脚下均为连续可走面，剧情显现时才临时启用点击Collider。
- PlayMode逐段验证“初到0只可见→剧情后3只可见且旧UI打开→确认后未选2只立即消失→所选1只延时消失”，最终候选0只、运行0错误；Unity编译0错误，4项序章／选择合同测试通过。

## P3.66 初契UI回退与测试存档修复（程序，2026-09-14）

- 因完整三卡新版存在交互问题，`StarterSpiritChoiceUI`布局与交互恢复为原有顶部标题、底部三按钮、信息区和二次确认结构；保留帕鲁模型头像、场景三宠直显、稳定ID和选择存档合同。
- “确认结契后停在主界面”的根因不是旧UI按钮：当前槽位为`starter='' / NotStarted`，但`spiritRoster=1 / active=1`，提交被`RosterNotEmpty`合同拒绝。该不一致由此前验证结束时只清序章字段、未同步清测试灵宠造成。
- 当前槽位已修复为一致新档状态（starter空、roster 0、active 0）；PlayMode实际执行“打开旧UI→选择火花狸→确认结契”后，UI正常关闭并写入`StarterChosen`，最终运行0错误。验证后再次恢复干净新档。

## P3.65 三宠直显与详尽三选一UI（程序＋策划，2026-09-14）

- 按当前表现要求取消序章三枚程序球体：`StarterSpiritChoiceWorldEntity`不再生成`ContainerVisual`或遮挡模型，火花狸、响响鸮、弹弹胶三只帕鲁原型在选择前直接站场；初契后所选灵宠进入附着演出，另外两只继续留在照料员旁。
- 从当前Kitsunebi、WizardOwl、SheepBall Prefab离屏渲染三张512×512正面模型卡面，接入`ProjectRUITheme`替换旧插画头像。
- `StarterSpiritChoiceUI`由底部按钮条升级为完整三卡界面：每张卡常驻展示模型、名称、属性方向、性格与战斗倾向，支持鼠标悬停高亮、数字键1／2／3、步骤1／2提示和独立二次确认层。
- PlayMode确认三只场景候选各有可见Renderer且不存在程序球体；三卡浏览与确认层截图验收通过，运行0错误、Unity编译0错误，三项初契选择合同测试全部通过。

## P3.64 序章正式场景恢复与全链烟测（程序＋关卡，2026-09-14）

- 以保留策划手改的HIGHLANDS `Demo 1.unity`为视觉源，重新生成正式`StarterPrologue.unity`；恢复五组中文根层级、V5最新Guide、运行时系统、玩法标记、交互触发器和主相机，旧白盒场景另存备份。
- 恢复合并空地最终坐标、三块低矮岩石边缘构图、高草／灌木清理与敌人出生点净空；重新预烘`StarterPrologue_NavMesh.asset`，结果为629顶点，选择区、两只Monkey、刃铠灵和固定回家点均可采样。
- 重新接入三只初契原型、球形容器运行时表现、照料员惊慌、第一阶段`AgitatedMonkey.prefab`和第二阶段`PossessedHost_Ronin.prefab`。
- PlayMode烟测通过：三枚容器与照料员惊慌正常生成；两只Monkey均附着NavMesh；刃铠灵可进入第二阶段并非致死撤离；结算后场景保持在序章等待固定回家点，实际进入触发器可加载`ProjectRHome`。场景缺失引用0、Unity编译0错误、最终运行日志0错误。
- 测试产生的序章存档字段已恢复为新档状态，避免下次运行跳过开场。

## P3.63 序章散落容器、刃铠灵撤离与固定点转场（程序＋策划，2026-09-14）

- 初契揭示程序改为“照料员惊慌→三枚散落容器回应→远端刃铠灵→打开现有三卡选择”：照料员获得蜷身颤抖占位表现，三只场景灵宠在选择前由运行时球形容器遮蔽；确认后所选灵宠短暂显现并进入首次附着，另外两枚容器继续留在照料员身边。
- 远端威胁预告复用Ronin原型并改用橙红领地威吓；第二阶段所有玩家可见目标与Boss条改为“刃铠灵／凶势”，凶势归零后模型沿背离玩家方向后撤入林，不触发普通死亡、掉落、人宠分离或双昏厥。
- 战斗结算只保存`RescueCompleted`、开放回家路径并指向固定`HomeReturn`标记；删除战后2.4秒自动转场和重进自动转场，玩家实际进入固定回家点触发器后才加载`ProjectRHome`，家园确认玩家落点后仍提交`Completed`。
- 照料员带走另外两只的退场从初契确认后移到刃铠灵撤离后；里程碑改为“道路安全／凶兽退去，先返回家园”。既有`StarterProloguePossessedHost`类名、序列化字段和波次枚举暂作兼容墓碑，不再作为玩家可见设定。
- `Assembly-CSharp`与`Assembly-CSharp-Editor`均通过编译（0错误）；Unity编辑器桥接在域重载后未恢复连接，因此本批尚未完成新档全流程Playtest，保留对应待办。

## P3.62 序章第二阶段改为凶性刃铠灵（策划，2026-09-14）

- 序章不再用“摘星队新人强行共鸣后失控”解释第二阶段敌人；现有Ronin模型改作凶性大型灵宠“刃铠灵”的内部原型。其类人轮廓、甲片、面具状脸壳与刃器均按物种特征理解，不是人类武士，也没有被附身或操控。
- 刃铠灵设定为近期闯入道路的独居领地型灵宠，会先用刃器敲地、低吼和逼近驱赶入侵者；“凶”是行为倾向，不代表邪恶。附近两只Monkey受到威吓和战斗动静刺激后躁动，第一阶段双敌流程继续保留。
- 第二阶段三招机制骨架保留，策划语义改为直线刃风、横向冲步斩和一次近身踏地震荡；Boss条从“稳定值”改为“凶势”，表示其继续争夺领地的意愿。
- 凶势归零后刃铠灵踉跄后撤、警戒回望并跃入林地，不死亡、不昏厥、不掉落、不结契；删除原人宠分离、双昏厥和“双方安全”收尾，改为“凶兽退去，道路暂时安全”。
- `RescueCompleted→加载独立家园→确认落点后Completed`、第一波两只Monkey、三选一和首次附着合同均保持不变。当前仅同步策划权威、关卡详案、垂直切片计划和开发待办，未修改程序、Prefab或Scene。

## P3.61 序章事故揭示与散落容器初契策划（2026-09-14）

- 低坡后的剧情揭示从“看见照料员与三宠后立即三选一”改为四拍：先看见照料员蜷身护头、呼吸急促并回望危险，再顺视线看到翻倒行囊与三枚散落容器，随后短暂展示远端失控者，最后把控制权交还玩家主动靠近。
- 三枚球形灵宠容器不使用玩家可见专名，不照搬捕捉球规则；单颗不可拾取、不可投掷、不进背包，也不会因玩家碰到某一颗而直接选中对应灵宠。
- 玩家进入三枚容器共同覆盖的接近区域后，三枚容器依次回应，照料员从“别过来”转为“它们在回应你”，再打开现有三张常驻选择卡和二次确认。
- 选择确认后仅被选容器打开或化为光点，对应灵宠主动靠近主角；另外两枚保持关闭并由照料员看护、带回家园。当前仅同步GDD、新手关详案和开发待办，未修改程序、Prefab或Scene。

## P3.60 动态角色默认禁用剔除（2026-09-14）

- 新增`DynamicCharacterRendering`统一运行时策略：所有接入的玩家和怪物Animator默认使用`AlwaysAnimate`，SkinnedMesh默认关闭动态遮挡剔除并启用离屏更新，先保证角色不再出现“只有影子／间歇消失”。
- 玩家在`PlayerAnimator.Awake`及运行时换模型的`SetAnimator`应用；普通怪物由`MonsterPrefabs.InstantiateMonster`应用；使用KayKit驱动的序章Monkey、Ronin等在`KayKitLocomotionDriver.Awake`兜底应用。
- 新增独立契约测试覆盖三项默认值。该策略会增加离屏角色的动画与蒙皮开销，正式性能验收前保留，后续再按角色重要性恢复分级剔除。
- 按本轮要求未连接Unity MCP进行运行验证；编辑文件静态检查0错误。

## P3.59 序章躁动敌宠Game视图可读性修正（2026-09-14）

- 精确复现“Scene中有本体、Game中同位置只有影子”：`TutorialAgitatedSpirit_1`已生成且位于主相机视锥和NavMesh中，Renderer、材质与Forward Pass均启用；根因是HIGHLANDS场景遮挡数据把该位置的动态SkinnedMesh误剔除，而阴影通道仍继续绘制。
- 在`AgitatedMonkey.prefab`关闭`SkinnedMeshRenderer.allowOcclusionWhenDynamic`，不关闭场景相机的全局遮挡剔除；原位置逐帧验证后Monkey本体立即恢复，影子仍正常保留。契约测试新增该设置断言防回退。
- 针对移动／攻击中仍可能间歇复发，进一步启用`SkinnedMeshRenderer.updateWhenOffscreen`并把Animator剔除模式改为`AlwaysAnimate`，避免蒙皮与动画变换因短暂不可见而冻结；连续约27秒、多轮移动和攻击复验两只均未再消失。
- 只对序章躁动敌宠把CharacterController／NavMeshAgent半径从0.4米提高到0.6米，并为两只实例设置不同避障优先级；不修改全局普通敌人。复验接敌后中心距约3.4米，两只均能独立辨认。
- 提亮Monkey苔绿／暖橙程序纹理，加入低强度Emission并关闭接收阴影，保留投射阴影，避免背向主光时融入地面。
- Game视图复验两只Monkey同时清晰可见、均在NavMesh；Unity编译0错误，精确序章Scene契约断言通过，临时验证截图已删除。

## P3.58 序章失控宿主Ronin与Monkey纹理材质接入（2026-09-14）

- 第二阶段`TutorialPossessedHost`通过序列化表现Prefab替换为此前选定的Ronin武士；稳定值、三招、分离结算、软边界与存档流程均保持原合同，Prefab缺失时仍回退原胶囊白盒。
- Ronin FBX改为有效Humanoid Avatar，新增宿主专用`PossessedHost_Ronin.prefab`与Animator Controller，复用KayKit Idle／Run／单手斜斩动画；Root Motion关闭，现有AI位移继续驱动动画，三种宿主攻击均触发斩击表现。
- Ronin继续使用既有五套URP材质，运行时只实例化并脉冲武器材质的青蓝Emission，不污染共享Boss视觉Prefab材质。
- Monkey两套纯色验证材质补入256×256程序纹理：主体改为深浅苔绿毛色层次，强调部位改为暖橙层次，并保留URP Lit低光滑度；该纹理用于内部可读性验证，不视为正式美术贴图。
- PlayMode验证第一阶段两只Monkey均加载纹理并位于NavMesh，第二阶段Ronin可见、Humanoid和控制器有效、宿主位于NavMesh且武器能量脉冲正常；精确序章Scene契约断言通过，Unity编译0错误。

## P3.57 序章躁动敌宠Monkey原型接入（2026-09-14）

- 从帕鲁模型包导入`Monkey`作为序章唯一躁动敌宠原型；第一波两只近战敌人复用同一`AgitatedMonkey.prefab`，三只初契宠与失控宿主外观不变。
- Monkey FBX配置为有效Humanoid Avatar，使用独立苔绿／赤红URP Lit原型材质；源模型没有贴图和动画，材质仍属验证占位。
- 从KayKit资源包复用Humanoid `Idle_A`、`Running_A`和`Melee_Unarmed_Attack_Punch_A`，建立`AgitatedMonkey.controller`；实际位移驱动Idle／Run混合，近战预警开始时触发拳击动作，Root Motion关闭。
- `EnemyBase.Spawn`新增保留旧签名的Prefab覆盖重载，`StarterPrologueTrainingEncounter`只向序章第一波传入专用Monkey Prefab，不修改全局`MonsterPrefabs`，避免影响普通关卡、精英、Boss与召唤物。
- 俯视可读性验证后将视觉高度调至约1.62米，仍处于2米CharacterController内。PlayMode验证两只Monkey可见、Humanoid有效、Speed参数1.55～1.71、`isOnNavMesh=true`，Attack触发后进入目标状态过渡；相关序章Scene契约测试通过，Unity编译0错误。

## P3.56 序章战斗区关卡设计整理（2026-09-14）

- 按5米闪避距离与俯视镜头可读性整理约26×12米战斗内场：保持入口到失控宿主的中央主轴完全开阔，将3块低矮岩石由无序散点调整为两左一右的边缘三角构图，分别承担左侧近敌周边层次、北侧边界提示与右侧入口框景。
- 清除两处躁动灵宠和失控宿主出生点2.2米范围内的14株矮草／三叶草，避免生成时模型脚下穿插植被；外围地表陈设保持不动。
- 重烘`StarterPrologue_NavMesh.asset`后为629顶点；战斗入口、两处躁动灵宠出生、失控宿主出生与家园出口均可采样，入口至左右敌人与右敌至宿主三条路线均为`PathComplete`。
- PlayMode复验两只躁动灵宠生成正常且`isOnNavMesh=true`；清理了本轮及上一轮生成的4张临时场景截图，未把验证图片留在Unity资产中。

## P3.55 序章战斗内场陈设清理（2026-09-14）

- 清空合并空地北半区约26×12米战斗内场的碰撞陈设：删除10棵树（`Fir_1`／`Fir_2`／`Fir_3`／`Fir_Dead_1`／`Fir_Dead_2`／`Fir_Tall_2`，最高13.6米）、15块高过1.5米的岩石、1根`Pillar`和1根`Log_1`，共26件。
- 保留3块低矮岩石作掩体与视觉锚点：西侧`Stone_2 (4)`(-4.1, -6.2)、中央`Stone_4 (3)`(-0.7, -9.5)、东侧`Stone_3`(14.4, -7.7)，高度1.2～1.4米，不遮挡俯视镜头下的角色轮廓。
- 清理视觉噪点：删除内场7丛灌木和21株高过0.8米的`Grass_1`；保留37株矮草与三叶草、野花、碎石共83件地面陈设，最高1.0米。
- 陈设改动后重烘`StarterPrologue_NavMesh.asset`，顶点数由868降至573（障碍减少后可行走面更连贯）；战斗入口、两处躁动灵宠出生、失控宿主出生、选择区与家园出口均可采样到NavMesh，`躁动灵宠2→战斗入口`寻路为`PathComplete`。
- PlayMode复验两只躁动灵宠生成正常且`isOnNavMesh=true`，运行期0报错。

## P3.54 序章救援与战斗空地合并（2026-09-14）

- 按策划在`StarterPrologueLayoutV5_BlockoutGuide`上的手改收口布局：独立战斗空地`AREA_E`、`LABEL_E`与自动越坡示意已删除，救援空地放大到约29×22米并同时承担三选一和同场两阶段战斗；场景实例的修改已Apply回Guide Prefab，Prefab与场景重新成为同一基准。
- 重排战斗节点到合并空地北半区：战斗触发器`(3.76, -12.5)`、躁动灵宠出生`(-7, -8)`与`(8, -8)`、失控宿主出生`(8, -4.5)`、首次附着门`(3.76, -16.3)`、失控区续玩点`(-3, -15.8)`、家园转场`(22, -8)`；全部落点经射线验证为y≈5.0平坦地面，不再像旧坐标那样压在y=8～15的岩石高地下。
- 通往战斗区的通道改为家园出口，并按岩壁实际位置从22米缩短到约9.6米；战斗触发器体积由18×3×8改为30×3×3，覆盖空地全宽且与照料员保持距离，避免选择阶段误触发后无法重入。
- 序章NavMesh改为编辑器预烘`Assets/1Game/Scenes/StarterPrologue/StarterPrologue_NavMesh.asset`（868顶点，覆盖37×25米合并空地与家园出口），`StarterPrologueSceneBootstrap`只在预烘数据缺失时退回运行时构建；`DungeonNavMeshRuntime`新增按世界体积收集的重载，Edgar地牢仍走原有的子物体收集。
- 修复出包隐患：旧的运行时烘焙会对HIGHLANDS岩石、树木和圆木的MeshCollider报“does not allow read access”，在编辑器可用但打包后丢失障碍；改为预烘后运行期0报错。
- `FirstAttachmentGate`去掉墙体：合并后没有通道可封，7米白盒墙在29米宽空地上形同虚设，改为关闭MeshRenderer与BoxCollider，只保留脚本发布“赶往失控区救人”目标点，准入仍由`BeginEncounter`的存档进度判定保证。
- PlayMode验证两只躁动灵宠生成于新点位且`isOnNavMesh=true`，战斗入口、选择区与家园出口均可采样到NavMesh；Unity编译0错误，契约测试594通过，4项失败均为既有且与本次无关（3项URP`ShaderStrippingReportTest`换行差异、1项`SpiritTalentTreeVisuals`Sprite销毁）。

## P3.53 序章出生点与战斗导航修正（2026-09-14）

- 将`StarterPrologue`玩家出生标记从偏离V5引导约22米的位置重新对齐到`MARKER_PlayerSpawn`世界坐标`(1.11, 6.21, -51.85)`。
- 修正V5正式环境接入后训练战运行时NavMesh为空的问题：序章只为引导定义的战斗空地生成临时导航底板，不扫描HIGHLANDS不可读美术网格；首次附着后进入战斗区可正常生成2只躁动灵宠，后续失控宿主出生点也已覆盖。
- PlayMode验证两只躁动灵宠均生成且`NavMeshAgent.isOnNavMesh=true`，战斗入口、两处近战出生点与失控宿主出生点均可采样到NavMesh；Unity编译错误为0。

## P3.52 初契三宠与序章Boss原型模型接入（2026-09-14）

- 在`Assets/1Game/ArtRes/Package/Character/palu/`导入Kitsunebi、WizardOwl、SheepBall与Ronin四套FBX蒙皮模型及贴图，统一配置Generic Avatar、关闭动画导入、关闭相机／灯光导入，并建立ProjectR URP Lit材质和独立视觉Prefab。
- `StarterPrologue`三只初契场景实体保留原选择组件与SphereCollider，关闭白盒MeshRenderer并分别挂接火花狸、响响鸮与弹弹胶模型；按原展示位缩放、落地并转向序章来路。
- `MonsterPrefabs.Boss敌人Prefab`改接Ronin视觉Prefab；Prefab内部缩放与现有Boss生成时的2倍根缩放配合，使最终模型约3.6米高，继续复用现有`EnemyBoss`、CharacterController和战斗逻辑。
- 四套源模型只有骨架与蒙皮，不含AnimationClip；当前Animator Avatar有效，但Idle、移动、攻击、受击与死亡动画仍待制作或重定向。Unity编译错误、场景缺失引用和资产缺失引用均为0。

## P3.51 初契序章V5正式场景集成（2026-09-14）

- 以已在HIGHLANDS `Demo 1.unity`中搭建的环境、Terrain、Lighting、Global Volume与反射探针为新场景底稿，迁入原`StarterPrologue`的Systems、Gameplay、UI、Bootstrap、主相机和序章玩法对象，并替换正式`Assets/1Game/Scenes/StarterPrologue.unity`；原场景备份到`Scenes/Backups/StarterPrologue_PreV5_20260914.unity`。
- 以两个场景中的`StarterPrologueLayoutV5_BlockoutGuide`为唯一对齐基准：先应用Guide根节点矩阵差，再逐项将玩家出生、照料员、三宠、落地选择触发器、两只躁动灵宠、失控宿主、战斗入口与家园转场节点对齐到V5标记；旧V3可见环境与112个旧几何碰撞停用。
- Hierarchy收敛为`00_序章环境_V5`、`01_光照与氛围`、`02_运行时系统`、`03_序章玩法布局_V5`与`99_设计引导_默认关闭`五个中文根；环境、光照、反射探针、运行时构建器和Guide等安全对象同步使用中文语义命名，名称可能被代码查询的具体玩法节点保留兼容英文。删除HIGHLANDS演示Controller及全部关闭的演示根，保留源场景中启用的环境内容。
- 更新序章Scene契约，从强制依赖LayoutV3改为依赖V5 Guide。Unity编译与缺失引用检查为0，155项ProjectR契约测试通过；PlayMode烟测确认玩家、HUD、相机和序章Bootstrap正常启动。出生镜头右侧仍有大型岩体遮挡，留待环境构图人工调整。

## P3.50 场景物体贴地与编辑器菜单精简（2026-09-11）

- 新增`ProjectR/选中物体贴地`编辑器窗口：支持将多选场景物体按包围盒底部或Transform轴心贴合TerrainCollider／普通Collider，提供地面层、离地偏移、最大检测距离、法线对齐与Undo；执行结果直接显示在工具面板内，不弹出需手动确认的对话框。可勾选启用数字键`1`快捷贴地，快捷键仅在工具窗口打开期间响应。
- 删除已无ProjectR专属职责的通用相机定位器，以及字体资产生成完成后不再需要的一次性TMP导入／字体生成脚本；保留配置总控、战斗配表、序章测试与契约测试等当前开发工具。
- 同步移除`UGuiKit`对已删除字体生成菜单的提示，缺失字体时改为检查项目内置字体资产。Unity编译错误为0。

## P3.49 初契序章V5开场与揭示节拍（2026-09-09）

- 新增非阻塞`StarterPrologueDialogueHUD`与`StarterPrologueOpeningBeat`：全新序章取得操作权后显示“主角｜照料员怎么还没来……”，目标同步改为“沿林间小路去看看”；已有初契检查点不会重播开场。
- `StarterPrologueChoiceTrigger`加入可序列化`revealFocus`。玩家进入低坡落地后的选择区时，先聚焦照料员并显示“先别过来！它们被失控的能量惊到了！”，再沿用0.7秒失控宿主预告并打开三宠选择；全程不暂停移动，只有选择界面打开后暂停。
- 在`StarterPrologueLayoutV5_BlockoutGuide.prefab`新增`MARKER_RevealLanding_ChoiceTriggerCenter`与挂接备注，建议在局部坐标`(0,-0.92,50)`放置选择触发器并把镜头焦点指向`MARKER_Caretaker`；仍不向范围Prefab加入玩法脚本或碰撞。
- Unity编译错误为0，155项ProjectR契约测试通过。临时新档烟测确认开场字幕、39米移动目标、照料员警告、异常预告到初契选择的顺序正常，退出PlayMode后正式存档恢复。

## P3.48 初契序章独立家园转场合同（2026-09-09）

- 新增`StarterPrologueHomeTransition`：宿主救援后先保存`RescueCompleted`，短暂展示收尾反馈后加载独立`ProjectRHome`；从救援检查点重进序章会自动重试转场，加载失败时不提前提交完成。
- 新增最小技术落点场景`Assets/1Game/Scenes/ProjectRHome.unity`与`ProjectRHomeSceneBootstrap`，只提供30×30米可替换地面、玩家出生、相机和公共系统，不冻结家园布局；只有该场景成功启动并确认玩家落点后才幂等写入`Completed`。
- V5目标HUD将救援后阶段改为“转场·家园”，开发计时器跨场景保留并将新档目标从旧7～9分钟调整为6～7.5分钟，以家园到达提交作为结束节点。
- 修正序章场景契约对当前`StarterPrologueLayoutV3.prefab`依赖的过期断言。Unity编译错误为0，154项ProjectR契约测试通过；临时新档烟测确认`StarterPrologue→ProjectRHome`、`RescueCompleted→Completed`、玩家／相机生成及退出PlayMode后正式存档恢复均正常。

## P3.47 初契序章V5白盒范围引导Prefab（2026-09-09）

- 新建`Assets/1Game/Prefabs/Tutorial/StarterPrologueLayoutV5_BlockoutGuide.prefab`，不修改或覆盖LayoutV3。
- 用彩色线框标出18×14米林间会合点、38×7米移动教学通道、约1米自动越下低坡、20×15米救援空地、7米宽连接通道和24×20米同场两阶段战斗空地。
- 标出玩家出生、照料员、三只幼宠、玩家战斗入口、两只躁动灵宠、失控宿主和家园转场建议点；26个直接引导子节点为`EditorOnly`，Prefab根节点保持普通标签，方便用户在根下新增会参与构建的白盒几何。全部引导对象无碰撞、无玩法脚本。
- 新增6个专用引导材质与俯视施工图，并在Prefab Mode中打开供手工白盒搭建。Unity编译错误为0；本轮未接入Scene、NavMesh、战斗流程或正式环境。

## P3.46 初契序章林间会合点开场V5（2026-09-09）

- 参考《Kena: Bridge of Spirits》“受控陌生通道完成基础教学→进入明亮开阔自然场景→遇见关键伙伴”的开场节奏；只借鉴空间揭示，不引入脉冲机关、收集或解谜教学。
- 序章出生点由家园改为旅途中的林间会合点。主角在简易路标与旅行包旁说“照料员怎么还没来……”，随后沿唯一林间通道完成移动和镜头教学。
- 保留约1米自动越坡揭示、20×15米救援空地和24×20米同场两阶段战斗；删除坡侧回程、家园出生区以及序章地图与家园的物理连接。
- 救援结束先写`RescueCompleted`，随后淡出加载独立家园场景；家园落点确认后才写`Completed`。转场失败或退出时应从救援结果恢复，不重打宿主。
- 将首次玩家目标时长从7～9分钟调整为6～7.5分钟。同步Canvas、GDD、新手关、世界设定、结契系统、垂直切片、开发待办、测试清单和美术基准；本轮未修改Unity。

## P3.45 初契序章关卡重设计V4（2026-09-09）

- 按玩家草图将开场改为主角自言自语“照料员怎么还没来……”后直接取得操作权，沿两列树群之间的唯一道路完成移动与镜头教学。
- 道路末端新增策划层面的约1米低坡揭示：玩家持续前进即可自动越下，落地后才完整看见照料员、三只幼宠与远端异常；不增加跳跃按键、精确起跳或跌落伤害。
- 空间收敛为一条主路和两个功能空地：20×15米救援空地承担人物构图、三选一和首次附着，24×20米失控空地连续承担躁动灵宠与宿主两阶段战斗。
- 战后不解锁新捷径，玩家从低坡侧方缓坡接回原路返回家园；旧LayoutV3和环境V10仅作实现回退，不作为V4布局依据。
- 更新V4交互Canvas、GDD、新手关、开发待办和测试清单。本轮仅修改策划，未改Unity Scene、Prefab或代码。

## P3.44 初契序章开场动机修订V3（2026-09-09）

- 主角不再偶然路过并临时获得灵宠：主角本来就想找到共同探索的伙伴，并与照料员约好当天和火花狸、响响鸮、弹弹胶第一次见面。
- 新增约1分钟轻量开场：“家园小空地等待→照料员迟到→沿路寻找→发现事故”，让危机成为双方实际确认选择的契机，而不是初契动机本身。
- 家园首版收敛为屋外生活空地、整备桌、少量种植地、训练木桩和出口；明确不要求制作可进入室内、完整小镇、居民群或额外日常任务。
- 同步GDD、世界设定、灵宠结契、新手关、垂直切片计划、开发待办和测试清单。本轮仅修改策划，当前Scene、Prefab与流程代码仍从家园外缘遇险路段开始。

## P3.43 初契序章环境层级重构V10（2026-09-09）

- 将V1～V9按迭代编号堆叠的活动环境对象迁入`Environment_Art_V10`语义层级：全局地表／道路／FX，以及家园外缘、救援空地、失控空地、回家终点四区的植被与道具。
- 旧迭代根节点统一收纳到关闭的`Environment_Legacy_Passes_Disabled`作为回退；流程几何、触发器、敌人出生位、隐藏碰撞和标记点不参与本次重构。
- 不生成或修改LOD；完整保留HIGHLANDS Prefab自带的101个LODGroup与嵌套Prefab连接。全部共享材质启用GPU Instancing，无风材质Renderer继续静态批处理。
- 117个风动Renderer取消`BatchingStatic`，避免静态合批破坏对象空间风动；其余122个活动环境Renderer保持静态批处理。V10共239个活动Renderer、22个共享材质、零活动环境碰撞。
- 运行检查确认地表、三段道路、树木、倒木、生活道具和异常痕迹世界坐标未改变；Unity编译、运行错误和缺失引用均为0。

## P3.42 初契序章HIGHLANDS叙事陈设V9（2026-09-09）

- 按环境美术“叙事分组而非随机散件”原则新增3组低密度生活痕迹：家园外缘的补给车／木桶／木箱，照料员旁的掉落行囊，以及回家终点侧方的水槽／水桶／木柴。
- 三组分别表达“主角沿有人维护的熟路回家”“照料员途中突遭事故”“终点属于有人生活的聚落边缘”；不新增文字、交互、收集物、营地剧情或宏大世界设定。
- 新增7个HIGHLANDS生活道具和7个ProjectR URP适配材质；所有道具关闭自身碰撞与脚本、启用静态批处理，并远离主要通路、三宠选择位、宿主出生位和失控战斗区中央。
- V9新增17个Renderer、约9142顶点／7860三角形；所用纹理均为1K～2K、POT、启用MipMap并压缩，所有Prefab含LODGroup。Unity编译、运行错误和缺失引用检查通过。

## P3.41 初契序章HIGHLANDS植被风动V8（2026-09-09）

- 新增ProjectR自有`StylizedFoliageWind` URP Shader，为HIGHLANDS树叶、灌木、草和蕨类提供双频轻微摆动、Alpha裁切、双面渲染、主光／环境光和同步阴影Pass。
- 六类适配材质分别设置低幅度风力：树冠最慢最弱，灌木居中，草蕨略快；不接原包Built-in Shader或全局风脚本，也不改模型、碰撞与关卡逻辑。
- 当前V6环境中117个材质槽使用风动Shader、149个材质槽继续使用URP Lit；Shader支持、Unity编译、运行错误和缺失引用检查通过。
- 风动只用于消除静态塑料感，后续实机仍需确认树冠摆幅不会干扰敌人轮廓、软边界或攻击预警。

## P3.40 初契序章HIGHLANDS地表与可读性V7（2026-09-09）

- 新增`Highlands_GroundMoss_PR` URP材质，以包内`Moss_Albedo／Normal`统一四个功能区和连续底景，替换重复感明显的Garden地表Shader；降低法线强度和光泽，保留暖绿但减少黄褐噪点。
- 在不增加对象数量的前提下放大HIGHLANDS草蕨、灌木、小石、边缘岩石和花菇点缀，增强60度斜俯视下的软边界可读性；V6仍保持212个活动Renderer和零活动碰撞。
- 原`Env_AnomalyScar_*`明确改名为`Env_AnomalyTrace_VisualOnly_*`，缩短尺寸并将不透明亮青改为42%透明；它们只提示失控能量残留，不是机关、伤害区、收集物或交互节点。
- 实机检查确认地表与HIGHLANDS植被风格更统一，异常痕迹不再形成醒目的三叉标记；Unity编译、运行错误和缺失引用均为0。

## P3.39 初契序章HIGHLANDS环境替换V6（2026-09-09）

- 接入用户补充的`HIGHLANDS - Stylized Environment`资产，仅取森林树木、灌木、草蕨、花菇、岩石和圆木；不迁入Demo场景、交互脚本、营地／女巫屋、后处理、天空或项目级渲染配置。
- 原包材质依赖Built-in版`Raygeas/Highlands` Shader，且当前导入内容未包含说明中的URP Shader解包文件；新增11个ProjectR自有URP Lit适配材质，保留原模型／贴图并统一暖绿、深木褐、低光泽和双面裁切规则。
- `StarterPrologueLayoutV3.prefab`新增独立`Environment_Dressing_V6_Highlands`层：18棵HIGHLANDS树替换Kenney树，58组HIGHLANDS草石灌木替换V5软边界，另补11块边缘岩石、6组花菇点缀和1根倒木。
- V1仅保留连续草地底景，V3仅保留三段软边道路，V4保留事故物件，V5与旧Kenney树／圆木关闭作为回退；V6全部碰撞关闭，不改变原隐藏边界、路径开口、战斗净空和流程触发。
- V6共212个活动Renderer，全部使用`Universal Render Pipeline/Lit`并标记静态批处理；Unity编译、运行错误、缺失引用和活动碰撞检查通过。

## P3.38 初契序章环境占位V5软边界（2026-09-09）

- 沿27段现有隐藏碰撞线自动铺设58组低矮草簇、普通灌木和小石，使玩家在接触边界前能看到自然轮廓；路径开口继续由原分段碰撞保留。
- V5全部装饰关闭自身碰撞，只负责提示原白盒边界，不改变玩家可行走范围、战斗区尺寸、敌人导航或软边界脚本。
- 实机检查确认救援入口仍清楚，失控区中央保留完整战斗净空；边缘草石环不会遮住三宠、敌人出生位、宿主预警或回家出口。
- V5新增约5.1万顶点、174个Renderer；V1、V3、V4、V5共463个纯环境Renderer已标记静态批处理，不将灯光或流程对象错误静态化。
- Unity编译、Shader、运行错误、缺失引用和NavMesh连通检查通过；正式阶段仍需将重复小簇合并为更少的草石组合Prefab并做真机构建性能测试。

## P3.37 初契序章环境占位V4（2026-09-09）

- 新增ProjectR自有`PrologueSoftPath`轻量Shader，使用道路Mesh顶点色控制横向与首尾透明度；三段道路现在从中心泥土逐步过渡到草地，不再以硬边多边形或椭圆块结束。
- Kenney道路方砖继续只作评估资源，不接入运行场景；V4道路Shader不采样额外贴图、关闭深度写入，保持低成本且不参与碰撞。
- 从同一CC0子集补入低面数圆木，替换浅色程序圆柱并统一为ProjectR深木褐；首次附着门仅隐藏红色白盒网格，原阻挡与解锁脚本不变。
- 救援空地加入掉落软布和小水壶两个无碰撞叙事占位，说明照料员是在途中突遭事故；二者不可交互、不发光、不形成收集提示。
- Shader支持、V4无碰撞环境层、三段NavMesh连通、Unity编译、运行错误和缺失引用检查通过。
- 当前道路仍缺正式地表纹理与坡差，软布／水壶仍是简化几何，后续只替换表现资源，不新增教学规则。

## P3.36 初契序章环境占位V3与CC0树木（2026-09-09）

- 检查现有树木资源后排除高面数、阴暗扭曲的`Chinese Painting URP`古松，以及带明确樱花特征的Garden树，不为复用而偏离轻松圆润方向。
- 从Kenney Nature Kit 2.1选择性导入5棵树与5块道路模型作为评估子集；原包为CC0，来源、镜像和许可证已随资源归档，不导入完整样例和无关道具。
- 实际接入5类96～324顶点的低面数树，替换18棵程序圆冠树；按ProjectR暖绿基准重配三档鼠尾草叶色和深木褐树干，旧程序树保留为关闭状态回退。
- Kenney模块道路因方砖边界明显，仅保留在第三方评估子集中，未接入关卡；V2椭圆泥块改为3条可编辑不规则带状Mesh，并使用半透明粗糙泥土覆盖材质减弱硬边。
- V3新增可见环境合计约3242顶点且无碰撞；新档实机确认出生至救援、失控区内部与战后回家NavMesh不变，Unity编译、运行错误和缺失引用均为0。
- 当前低面数树与道路带可用于布局和遮挡验证，不自动升级为正式ProjectR美术资产；正式树冠细节、风动和地表混合仍需后续制作。

## P3.35 初契序章环境占位V2（2026-09-09）

- 隐藏三段矩形路径的可见网格但保留原碰撞，改用14块无碰撞椭圆泥土片叠出较松的林间路径；原白盒倒木同步替换为无碰撞圆木剪影。
- 序章主光保持暖黄绿方向光并启用中等软阴影，环境光改为低饱和绿褐三色渐变；回家终点增加克制暖光，不启用全局雾。
- 失控宿主附近加入3条亮青地面短痕和一盏低强度青蓝局部光；异常只集中在宿主周围，不扩成区域天气、机关或可交互节点。
- 隐藏旧完成门与立柱的可见网格，完成门碰撞和脚本保持不变；路径、异常痕和灯光均不参与NavMesh。
- 新档实机确认三宠选择、三处敌人出生位、出生至救援、失控区内部和战后回家连通未变；Unity编译、运行错误和缺失引用均为0。
- 当前椭圆泥土片、程序圆木和灯光仍是构图占位，正式阶段须换成手绘感地表混合、自然坡差与正式特效。

## P3.34 初契序章环境占位第一批（2026-09-09）

- 选择性复用Garden样例中的草地／碎石材质、景观石、普通冬青灌木和沿阶草；明确排除竹子、樱花、日式建筑、灯笼与完整样例场景。
- LayoutV3隐藏可见白盒墙体但保留原碰撞，加入60个无碰撞自然装饰实例，以石块、灌木和草簇勾勒路径与战斗区边缘。
- 增加无碰撞草地底景，消除四块地台漂浮在灰色背景中的割裂；用ProjectR自有平涂材质和程序圆冠树补出第一批林地剪影。
- 流程触发、三宠选择、三处敌人出生位和NavMesh不变；新档烟测确认出生至救援、失控区内部及战后回家路径连通，Unity编译与运行错误均为0。
- 本批仍是环境垂直切片占位，不代表正式地形、树种、光照或最终资源密度。

## P3.33 初契序章LayoutV3空间白盒（2026-09-09）

- 从原`StarterPrologueWhitebox.prefab`复制出可独立回退的`StarterPrologueLayoutV3.prefab`，原Prefab不覆盖、不删除。
- 按确认施工单重排家园外缘、救援空地、失控区和回家终点，加入三段约7米宽折线路径、宽失控战斗区、路径碰撞边界、非阻路倒木与四个镜头检查点。
- 初契门、遭遇触发、三处敌人出生位、分离结果点、检查点和完成门继续复用原流程组件，只调整空间位置；独立序章场景已切换为加载LayoutV3。
- 新档运行烟测确认三只初始灵宠、三处敌人出生位与关键区域加载正常，出生至救援、失控区内部及失控区至家园NavMesh连通；首次附着门在选择前有意隔断救援区与失控区。
- Unity编译与场景缺失引用检查均为0；当前墙块和直边地台仍是环境施工占位，下一阶段再用自然资产替换。

## P3.32 初契序章场景设计与概念基准（2026-09-09）

- 冻结家园外缘、遭袭草地、失控区和回家小径四段连续空间的画面功能、构图地标、遮挡、战斗净空与视觉叙事。
- 色彩脚本确定为“安全暖绿→局部异常青蓝→归途金绿”；异常不改变整片林地天气，也不增加门、结界、机关或可破坏节点。
- 新增16:9俯视布局色稿与Hades式游戏镜头氛围图，归档到美术基准目录；图中人形只作比例占位，不恢复任何已撤回主角设计。
- 补齐序章专用生成提示词、构图规则与最小环境资源包。下一阶段可优先评估Garden的地表、苔藓石、SSS植被、萤火虫和尘粒，但不搬入日式建筑、樱花／竹子或完整样例场景，并保留Unity Sample许可核查。
- 本批只完成策划设计与Concept Art，不修改Unity白盒、运行时资源或UX。

## P3.31 首次附着镜头聚焦（2026-09-09）

- 首次强制附着选择开启时，相机视野角在非缩放时间内由48度平滑收紧至44度，突出角色和附着选择；完成选择后自动恢复。
- 普通脱战换挂不触发镜头变化，避免频繁操作造成呼吸感干扰。
- 镜头聚焦不暂停玩法、不改变跟随目标，也可与现有序章短镜头并存。
- Unity编译、153项ProjectR契约测试及聚焦开关恢复烟测通过；实际舒适度待新档实机确认。

## P3.30 救援与回家里程碑反馈（2026-09-09）

- 失控宿主结算后显示“失控解除／人和灵宠都已脱离危险”，明确非致死救援结果。
- 抵达家园并提交完成后显示“初契完成”，副标题使用实际所选灵宠名称。
- 提示卡持续2.4秒，使用非缩放淡入淡出与轻微缩放，不暂停时间、不接收射线，也不增加确认按钮。
- Unity编译、153项ProjectR契约测试及提示卡内容／非阻塞烟测通过；实机阶段时序待继续确认。

## P3.29 序章HUD降噪（2026-09-09）

- 新增序章专用HUD可见性模式，仅保留生命、技能栏、灵宠附着、连招反馈、消息和序章目标。
- 序章隐藏旧探索区域、房间进度、敌人计数、碎片计数、小地图及图例，避免与新的阶段目标重复。
- 序章死亡由检查点重载处理，不再闪现通用“探索失败”面板；初契附着完成消息移除开发用“按1／2／3换挂”尾注。
- 强制脚本重载后实机确认五项无关HUD均隐藏，目标条上移至顶部；Demo1通用HUD不受影响。

## P3.28 序章阶段目标层级（2026-09-09）

- 顶部目标条由单行大字改为“小号阶段标签＋主目标＋距离”，区分遇险、初契附着、躁动灵宠、失控宿主、归途与序章完成。
- 目标文本变化时使用0.22秒非缩放淡入与轻微缩放，不暂停操作、不增加弹窗。
- 距离改为紧凑菱形标识，阶段标签由现有检查点和目标语义推导，不增加新的流程状态。
- Unity编译与153项ProjectR契约测试通过；目标条遮挡与阶段切换可读性待实机确认。

## P3.27 开发期每次全新序章（2026-09-09）

- 工具栏“启动游戏”和开发菜单“临时新档测试”统一改为静默创建全新槽位3，不再弹出占用存档确认框，也不继承上次测试进度。
- 正式三个槽位仍在启动前自动备份，退出PlayMode后自动恢复；发现上次残留的有效备份时先静默恢复再开始新测试。
- 场景存在未保存修改时仍保留Unity保存提示；备份目录损坏或清单缺失时停止启动，不以无提示为由覆盖正式存档。
- 实测确认菜单无确认框直接进入PlayMode，测试档为`NotStarted`、无初契灵宠和空契匣；退出后备份目录已清除。

## P3.26 首次附着动作卡UX（2026-09-09）

- 首次附着不再只依赖技能栏边框和一行悬停提示；新增底部三动作卡，直接展示LMB武器、Q灵息弹和SPACE身法对应的所选灵宠显化。
- 三卡可直接点击，也支持数字键1／2／3；原技能槽高亮与点击保留，首次必选时ESC仍不会跳过。
- Q卡明确显示“Q灵息弹”，避免把空泛的“术法”误解为尚未获得的技能。
- Unity编译与152项ProjectR契约测试通过；尺寸、遮挡和选择耗时待新档实机确认。

## P3.25 初契选择UX与Q技能时序（2026-09-08）

- 初契界面收敛为紧凑标题、三张常驻选择卡、底部说明和独立确认层；选择卡显示灵宠头像、属性与战斗倾向，并支持鼠标或数字键1／2／3。
- 修复初次进场时灵宠宿主尚未配置，导致选宠后未再次装配基础“灵息弹”的时序问题；初契提交后现在会立即补入Q槽。
- 投射物现在保存生成它的技能与槽位；灵息弹飞行后命中不再发布未知槽位`-1`，首次附着到Q可正常触发所选灵宠显化。
- 灵息弹尚未配置正式投射物Prefab时始终生成蓝白程序弹体，不再依赖`showDebugVisuals`；关闭调试显示的烟测确认可生成、可归因到Q槽。
- 投射物忽略释放者自身碰撞体和无伤害逻辑Trigger，并从角色前方0.75米生成，修复在序章遭遇触发区内出生后只闪一帧便回收的问题。
- Unity编译与152项ProjectR契约测试通过；界面尺寸、三卡辨识和Q附着显化待继续实机确认。

## P3.24 弹弹胶身法推开与打断（2026-09-08）

- 弹弹胶附着身法后，闪避终点的水系脉冲除伤害外会将范围内可受力目标向外推开，并打断当前攻击。
- 新增独立可选`ICombatImpulseReceiver`合同，不修改`IDamageable`；普通敌人与序章失控宿主接入，未实现该接口的Boss或特殊单位默认免疫受力。
- 失控宿主被打断时立即撤销当前预警，作废对应能量波、冲步斩或溢出环结算，不影响其稳定值和非致死结算。
- Unity编译、150项ProjectR契约测试和两类序章敌人位移／打断烟测通过；推开距离与战斗手感留待人工实测。

## 斜俯视相机与初契点击修正（2026-09-08）

- 序章与Demo1相机由70度鸟瞰调整为60度斜俯视，后移镜头并将垂直FOV由50收至48，增加接近Hades式的角色、道路与远景纵深。
- 保持固定朝向、现有移动映射和剧情聚焦机制，不在本轮引入自由旋转或越肩瞄准。
- 初契不再依赖场景物体点击：增加三个明确的界面选项与数字键1／2／3入口，同时保留新Input System场景射线选择。
- Unity编译与静态检查通过；斜俯视遮挡、战斗预警可读性和完整新档节拍待本轮实机继续验证。

## 新手序章临时新档测试入口（2026-09-08）

- 新增`ProjectR/开发工具/新手序章/临时新档测试`：启动前备份三个正式存档槽，临时使用槽位3创建新档并打开序章。
- 退出PlayMode后自动恢复原槽位文件和最近槽位偏好；异常中断时可用“恢复测试前存档”手动恢复。
- 备份放在工程`Library`下且不进入游戏资产；检测到旧备份时拒绝覆盖。
- Unity编译、149项ProjectR契约测试及菜单／场景／无残留备份检查通过；未在验证过程中改写正式存档。

## 序章技术合同文档同步（2026-09-08）

- `程序模块规划`从schema v8四态检查点和`2近战＋1远程`验证战更新为schema v9五态检查点及当前救援战合同。
- 开发待测项删除训练机关／打木桩残留，改为验证两只躁动灵宠的首次实战感和所选灵宠显化。
- 新手关正文统一使用“救援战”，不再将当前流程称为验证战。

## 新手序章流程对账（2026-09-08）

- 删除关卡策划中残留的“选宠前先打一只小兽”，不再额外增加当前白盒没有的基础战斗。
- 权威顺序统一为“遇险与失控预告→三选一→首次附着→两只躁动灵宠→失控宿主→返回家园”。
- 重新分配7～9分钟目标节拍，并同步GDD、垂直切片计划、开发待办和测试清单。

## P3.21 新手救援战软边界降噪（2026-09-08）

- 救援战边界在玩家处于内圈时保持近乎透明，进入外侧约32%区域后渐显并轻微脉冲。
- 越界回推由边缘内1.25米缩至0.35米，减少位置突跳；提示、无伤害和不重置遭遇规则不变。
- 增加中心／近边缘／越界透明度递增契约；Unity编译、149项ProjectR契约测试和实机透明度／回推距离验证通过。

## P3.20 初契前失控威胁预告（2026-09-08）

- 在初契弹窗前加入约0.7秒远景镜头，先展示失控宿主剪影与蓝色武器能量异常，再回到三只幼宠。
- 远景占位无碰撞、不可受伤且不运行AI；首次附着后进入救援战时自动移除，由正式宿主生成替代。
- Unity编译、148项ProjectR契约测试通过；实机确认镜头预告先于初契弹窗且两者不会同时抢占画面。

## P3.19 新手序章自然微光引导（2026-09-08）

- 在出生点至救援空地的25米路径上加入3点错落悬浮的自然微光，辅助玩家把视线转向前方。
- 微光无碰撞、不可交互，不属于收集物、机关或剧情设定；完成初契后自动销毁。
- 增加三点顺序与路径范围契约；Unity编译、148项ProjectR契约测试及实机可见性／自动隐藏验证通过。

## P3.18 新手序章阶段计时（2026-09-08）

- 新增开发用`StarterPrologueTimingTracker`，记录初契、首次附着、救援完成和回家完成的实际时间。
- 使用非缩放时间，将初契与附着界面的思考时间计入节奏验证；不在玩家HUD显示计时。
- 只有从新档`NotStarted`开始的完整会话参与7～9分钟目标判断，检查点续玩只输出本次会话时长。
- 增加包含7分钟与9分钟端点的边界契约；Unity编译、147项ProjectR契约测试及暂停计时烟测通过。

## P3.17 初契后照料员退场（2026-09-08）

- 新增一次性白盒退场：所选灵宠立即离开选择位并由HUD表示已附着，照料员带两只未选幼宠沿来路移动后缩隐。
- 序章不生成所选灵宠的世界空间跟随实体，也不建立护送队伍。
- 已有初契存档进入场景时直接恢复三只选择实体和照料员均退场的结果，不重复播放。
- Unity编译、146项ProjectR契约测试通过；实机确认退场对象固定为照料员加两只未选幼宠。

## P3.16 新手序章灵宠退场边界（2026-09-08）

- 初契确认后三只场景选择实体均退场：所选灵宠由技能栏／HUD表示已附着，另外两只由照料员带走。
- 序章战斗与回家路段不制作世界空间灵宠跟随，也不增加护送队伍或相关失败条件。
- 删除短暂加入的白盒同行组件与恢复逻辑；场景跟随留到局外家园阶段再评估。
- Unity编译和146项ProjectR契约测试通过。

## P3.15 新手序章救援／回家双检查点（2026-09-08）

- 序章进度升级为schema v9五态：未初契、已初契、已附着、救援完成、序章完成。
- 失控宿主结算后只提交`RescueCompleted`并开放回家路径；玩家抵达家园终点后才提交`Completed`。
- 战后退出或死亡可从新增`RescueResolvedMarker`恢复，并重建昏厥人宠占位，不再重打宿主。
- schema v8中数值3的旧完成档迁移时直接映射为v9正式完成，避免错误回退。
- 增加回家终点触发合同和迁移测试；Unity编译、146项ProjectR契约测试与战后恢复烟测通过。

## P3.14 新手序章白盒构图与镜头（2026-09-08）

- 将照料员和三幼宠从横向一排调整为面向玩家来路的三角构图，提高首次进入救援空地时的角色辨识。
- 初契选择、失控宿主登场和人宠分离加入短暂场景聚焦；镜头使用非缩放时间并自动恢复玩家跟随。
- 修正初契后的场景实体状态：所选灵宠离开选择位，另外两只留在照料员身边，符合后续随照料员回家的策划。
- 选择界面打开时隐藏常驻目标条，确认后恢复首次附着目标，避免模态标题与HUD重叠。
- 救援完成后回家终点显示轻量圆环与光束引导；Unity编译和ProjectR 145项契约测试通过。

## P3.13 新手序章引导与战斗边界（2026-09-08）

- 新增序章专用紧凑目标条，按进度显示救援、首次附着、两波战斗与回家目标，并显示目标距离。
- 救援战开始时按三处出生位建立低干扰圆环软边界；玩家越界只被放回边缘内侧并收到提示，不扣血或重置遭遇。
- 保留死亡后重载独立序章场景和`AttachmentChosen`检查点恢复，不重复开场、初契或首次附着。
- 实机确认目标条避开顶部区域标题、目标与距离正常更新、软边界可稳定放回玩家；ProjectR 145项契约测试与Unity编译通过。

## P3.12 失控宿主专属战斗（2026-09-08）

- 新增 `StarterProloguePossessedHost`，替换序章第二波的通用 `EnemyRanged`占位。
- 接入专属稳定值条、0.7秒直线能量波、约7秒一次冲步斩和半稳定值仅一次的近身溢出环。
- 稳定值归零通过序章专用回调完成人宠分离，不发布普通`EnemyKilled`，不触发掉落或人类死亡叙事。
- 白盒武器只显示蓝色属性能量流动，不在角色或武器上增加灵宠图标。
- Unity编译、145项ProjectR契约测试通过；实机确认三招执行、溢出环仅一次及非致死回调。

## P3.11 新手序章V2救援白盒（2026-09-08）

- 将序章战斗从“三训练靶→2近战＋1远程”改为“2只躁动近战灵宠→1名失控远程宿主”，删除训练靶和额外机关包装。
- `StarterPrologueTrainingRuntime`改为2目标与1目标的两阶段合同；失败仍按已附着检查点重载，完成后写入序章完成状态。
- 失控宿主结算后生成分离并双双昏厥的人宠白盒占位，不触发对话、逃跑或普通人类死亡叙事。
- 白盒Prefab区域改为家园外缘、救援空地、失控区和回家路径；加入照料员占位并保留三宠实体选择。
- 序章场景复用 `DungeonNavMeshRuntime`运行时构建导航，实机确认两只躁动灵宠均正常落在NavMesh。
- 更新Prefab与纯状态契约；ProjectR 145项契约测试、Unity编译和首波运行烟测通过。

## 摘星队、敌对单位与灵宠变体定案（2026-09-08）

- 首个人类团体暂定名为“摘星队”：追逐稀有灵宠、遗物与异常现象，前期制造麻烦，但成员立场不完全一致，为后续反转留空间。
- 敌对单位分为野生灵宠、由灵宠强化动作的敌对结契者、意识被异常灵宠／失衡回路压住的失控附身者三类。
- 失控附身被限定为罕见危险异常，不作为灵宠普遍天性，避免破坏伙伴定位。
- 同种灵宠可有普通花色与特殊变体；特殊变体最多改变1项技能侧重和1项性格权重，不形成全面更强的稀有上位。
- 新手序章改为摘星队强行共鸣造成能量倒流：成员与受困灵宠均失去自主，玩家三选一完成首次附着，先击退两只躁动灵宠，再直接打断失控者；连接断开后人宠分离并双双昏厥。
- 灵宠附着的世界空间视觉收敛为动作部位属性流动，不在角色或武器上显示灵宠图标；灵宠归属沿用现有技能栏／HUD挂件，失控仅增加属性流动异常、能量溢出和人物异常。
- 序章首战不增加额外机关或可破坏节点；失控者只使用直线波、冲步斩和一次溢出环，稳定值归零触发非致死人宠分离结算。

## 文档减负与新手关优先级调整（2026-09-08）

- 删除旧产品策划、重复GDD备份、ideas、阶段复查、旧功法／灵物／技术深档及旧版统一归档。
- 删除随代码目录中已冻结的旧灵物／功法配置指南、旧策划指针和Demo1功能快照。
- CHANGELOG与开发待办收敛为ProjectR现行内容，不再携带旧产品流水账。
- 风痕林谷停止继续实施；优先完成新手关具体内容策划，对账后再制作白盒。

## 首个秘境“风痕林谷”策划冻结（2026-09-08）

- 冻结九地点网络、改水生态传播、世界拾取＋安全点选择、苔苔团结契和可选灾相“盘根团块”。
- 当前仅保留为后续首区设计，不进入近期白盒制作。

## P2.20A～D 回路接管闭环（2026-09-08）

- 新增回路总览与战斗统计，迁灵保留天赋投资。
- 三宠支持拖拽／点击改附并实时预览 `1+1+1`、`2+1`、`3+0`。
- 三宠回路改为按所附玩家动作分段推进。
- 火花与回响成为有时限中间产物，HUD提示下一动作与剩余时间。
- G2程序闸门完成，具体战斗手感留待人工Playtest。

## P2.19C～F 灵宠天赋树（2026-09-07～08）

- 按指定美术基准落地三宠天赋树界面、角色插画、页签、节点语法和图标资源。
- 火花狸、响响鸮、弹弹胶各完成18节点分叉网络，支持All／Any前置、纯路线、融合与互斥终极。
- 完成九条标准5点BD与无天赋基线自动采样，修正首轮明显超标倍率。
- 高阶节点同时适配单宠与三宠运行路径。
- 操作节奏、响应、视觉噪声和目标选择手感延期到稳定战斗版本人工验证。

## 灵宠天赋长期成长定案（2026-09-07）

- 灵宠拥有永久开放、局内临时点亮的独立天赋树。
- 出战、击杀、探索、事件和关键胜利逐步开放更多节点。
- 安全状态允许免费重选当局点数；迁灵不清空永久开放或当局投资。
- “保留一项悟法”被天赋树长期成长替代；回路来源查看归入数值型战斗统计。

## 世界结构与家园术语收敛（2026-09-07）

- 采用单一自然幻想世界，只保留家园、秘境区域和当地异常。
- 删除诸界、栖庭、界门、界巢、归巢声等高解释成本设定。
- 玩家侧“洞府”统一为“家园”，定位为主角长期生活的小聚落。
- 主角旧设定撤回；只保留“主角独立战斗，灵宠附着动作并改变显化”的玩法关系。

## P3.3～P3.10 新手序章基础实现（2026-09-03）

- 建立初契序章断点恢复、三宠选择、首次附着强制引导和完成门。
- 创建 `StarterPrologueWhitebox.prefab` 与 `StarterPrologue.unity` 场景加载链。
- 建立三座训练机关和两近战一远程验证战的两阶段流程。
- 接入首批生成式HUD资源与紧凑技能栏布局。
- 现有内容属于功能骨架，具体关卡节奏、空间叙事和教学反馈待重新对账。

## P2.11～P2.17 首批三宠与HUD（2026-09-02～03）

- 火花狸、响响鸮、弹弹胶接入单宠三动作原型与三宠回路。
- 回路接入玩家真实命中、弹射与回响结算。
- 读取永久出战编队并支持HUD附着选择。
- 建立紧凑技能栏、灵宠徽标和回路反馈基线。

## P2.1～P2.10 回路底座（2026-09-02）

- 建立灵宠身份、三宠编队、脱战迁灵、声明式规则与可执行回路。
- 增加因果链、预算、防爆保护、来源归因、结构化结果与Legacy诊断旁路。
- 建立回路预览图、断点分析和永久灵宠存档契约。

## P1.1～P1.15 玩家动作拆分（2026-09-02）

- 拆分术法状态、效果执行器、普攻执行／效果体、身法命令与三类动作入口。
- `FeatureFlags.EnableCarrierRuntime` 接管默认动作路径，保留Legacy回退。
- 编辑器增加“启动游戏”快捷入口。
- 玩家可见文本取消线性“层”概念。

## ProjectR 产品重置（2026-09-02）

- ProjectR全面替代旧产品方向，当前Unity工程、Edgar、基础战斗、存档和工具继续作为宿主。
- The Archer仅选择性迁移角色骨骼／动画、普通怪物和合适Boss资产，不整体替换工程。
- 建立G1～G7切换闸门、模块规划、影响检查和四类权威文档。
