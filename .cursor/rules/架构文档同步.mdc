---
description: 改动游戏逻辑后同步程序架构、模块规划与影响记录
globs: Babylon/Assets/1Game/Scripts/**/*.cs
alwaysApply: false
---

# 架构文档同步（tech readme）

程序架构权威文档：`Babylon/Assets/1Game/Docs/程序_架构说明.md`
（`docs/game/tech/架构总览.md` 仅为指向它的重定向存根，勿在其中写正文。）

ProjectR稳定模块边界：`docs/game/architecture/程序模块规划.md`  
改动影响流程：`docs/game/architecture/变更影响检查.md`

## 规则

**在改动游戏逻辑后，必须同步更新上述架构文档的对应章节**，不要让它滞后于代码。触发同步的改动包括（不限于）：

- 新增 / 删除 / 重命名 / 重构 **类、接口、枚举、ScriptableObject**
- 变更 **核心设计模式**（单例 / 事件总线 / 工厂 / Provider 等）或**模块职责边界**
- 新增 / 修改 **`GameEvents` 事件**、`IDamageable` / `CombatStats` 等核心接口
- 变更 **关卡 / 房间 / 存档 / 战斗 / 大秘境 / 模块化技能** 的流程或数据流
- 新增 **房间类型 / 敌人类型 / 地图 Provider** 等可扩展点
- 变更ProjectR模块职责、允许依赖、P0～P5迁移阶段或G1～G7闸门

## 同步内容

- 更新对应章节正文（目录结构 §一、设计模式 §二、模块详解 §四、事件表 §五、数据流 §六、扩展指南 §八、解耦架构 §十 等）
- 若模块边界或迁移顺序变化，同时更新 `docs/game/architecture/程序模块规划.md`
- 跨文件程序改动按 `docs/game/architecture/变更影响检查.md` 记录主模块、受影响模块和改前／改后代码图结论
- 更新文首「最后更新」日期与「当前基线」快照（若基线变化）
- 若属重大改动，在 `docs/game/CHANGELOG.md` 亦留一笔

## 反例

❌ 加了新房间类型 / 新事件 / 换了地图系统，却只改代码不动文档 → 文档失真、误导后续开发与 AI。
✅ 代码与 `程序_架构说明.md` 在同一次改动内保持一致。
