---
description: 根据“程序／策划”范围词判断任务边界，范围不明确时先确认
alwaysApply: true
---

# 程序与策划范围确认

## 用户推荐范围词

- **程序**：修改或分析C#、运行时架构、接口、事件、存档、算法、测试、Prefab接线及技术文档。
- **策划**：修改或分析GDD、系统规则、玩法流程、内容规格、数值目标、策划配表及待办优先级。
- **程序＋策划**：同时处理实现与设计，并执行双方文档同步和对账。

## 判断规则

- 用户明确写出范围词时，以该范围为主，不擅自扩展到另一侧。
- 用户虽未写范围词，但目标明确（如“修编译错误”“设计器灵获取规则”）时，可按语义直接判断，不必机械追问。
- 同一句话可能同时改变玩法规则与代码行为，且用户未明确范围时，修改前先询问“只改程序、只改策划，还是两边同步”。
- Unity场景、Prefab、资源导入、表现配置或纯数值调整无法明确归类时，先说明可能影响并询问范围。
- 为理解问题可以跨范围只读检查，但未经授权不要跨范围写入。

## 同步边界

- **只改程序**：允许按项目规范同步实现现状、CHANGELOG和开发待办，但不得自行改动玩法规则。
- **只改策划**：不得顺带修改C#、Prefab、Unity资产或生成配置。
- **程序＋策划**：先确认设计权威，再改实现，最后同步GDD实现现状、技术架构、CHANGELOG和开发待办。

## 询问模板

> 这项改动同时可能影响程序行为和策划规则。你希望只改程序、只改策划，还是两边一起同步？
